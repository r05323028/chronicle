## 1. Baseline and assertion-level classification

- [x] 1.1 Capture current checkout baseline before edits: all Rust `#[cfg(test)]` modules/test functions, every `crates/*/tests/**` file, root `tests/**`, `scripts/tests/**`, acceptance scenario/check definitions, privileged runners, Multipass flow, CI callers, validation groups, and evidence/report fields. Acceptance: machine-readable inventory names each significant test/assertion and records current command, runtime, process/network use, and current P1/P2 obligation; baseline includes at least current 49 Rust test-bearing files, 8 crate integration files, 13 acceptance scenario IDs, and all embedded acceptance `cargo test`/repository-check invocations or explains drift.
- [x] 1.2 Create versioned migration ledger with columns for current path/assertion ID, purpose, current layer, target functional layer, portable/privileged requirement, crate/feature owner, public/internal surface, P1/P2 obligations, disposition, destination/replacement test ID, dependency impact, rationale, and verification command. Acceptance: every significant baseline entry has exactly one resolved functional layer and one environment classification; ambiguous entries block migration rather than defaulting privileged.
- [x] 1.3 Produce gate coverage matrix mapping every current `scenarios.toml` scenario, legacy check, P1/P2 requirement, release obligation, and retained evidence field to classified replacement tests. Acceptance: no required check is unmapped; P2 remains complete and P1 subset semantics remain explicit.
- [x] 1.4 Review ledger against `workspace-dependency-boundaries`, `validation/architecture.toml`, crate ownership docs, and Cargo dev edges. Acceptance: every shared fixture/helper has owner; no target plan introduces CLI lower-layer, protocol-to-builtins, session-to-WAL, common-upward, Aya leakage, or unclassified dev/build edge.
- [x] 1.5 Record deliberate decisions for current mixed suites, including `crates/chronicle-application/tests/{fake_vertical,fixtures_http_vertical,http_test_server}.rs`, `crates/chronicle-cli/tests/{cli_contract,privileged_signal}.rs`, `crates/chronicle-capture-ebpf/tests/{privileged_adapter,privileged_feasibility}.rs`, root HTTP acceptance driver, P1 capture-basic/replay/WAL recovery, and all P2 scenarios. Acceptance: each is marked stay/move/split/rootless/remain-privileged/delete-duplicate/helper-migration with responsibility or reliability reason; no directory-aesthetic-only move.

## 2. Classification and no-coverage-loss enforcement

- [x] 2.1 Add smallest machine-readable test catalog/metadata needed by validation to represent test ID, functional layer, environment requirement, owner, command, and gate membership; reuse existing TOML/standard-library tooling where possible. Acceptance: catalog validates duplicate/unknown IDs, missing owner/layer/environment, undefined command, and unmapped required gate checks without new dependency/framework.
- [x] 2.2 Add focused catalog/gate tests proving P1/P2 are selectors rather than categories, P2 includes required P1 subset where mandated, privileged requirement is orthogonal to functional layer, and required obligations cannot disappear during migration. Acceptance: deleting one required mapping fails deterministically with exact obligation/test IDs.
- [x] 2.3 Add static/meta-tests preventing portable Cargo/repository checks and product field assertions from being embedded in privileged executor/profile orchestration, while allowing environment probes, evidence validation, and selected test dispatch. Acceptance: representative current violations fail fixture tests before migration and pass only after moved assertions are mapped.
- [x] 2.4 Add architecture/helper ownership checks for any new root test support and dev dependencies. Acceptance: existing architecture validator still discovers optional/target-specific/dev/build edges and focused fixtures reject dependency escape hatches.

## 3. Unit and integration ownership migration

- [x] 3.1 Review all colocated Rust tests and keep local logic colocated; move only tests that demonstrably exercise public component contracts or subprocess/full-system behavior. Acceptance: ledger records each file as reviewed; no centralized `tests/unit` tree is created.
- [x] 3.2  Move/split portable WAL encoding, checksum, recovery, corruption, retention, and fault matrices from privileged scenarios into `chronicle-wal` integration/unit ownership using deterministic real WAL files and fixtures. Acceptance: replacement tests cover every displaced check and run rootlessly; privileged scenarios no longer invoke broad WAL Cargo suites.
- [x] 3.3 Move/split portable ETL/checkpoint/publication/restart/idempotence/corruption behavior into `chronicle-etl` and owning application/storage integration tests. Acceptance: copied-WAL corruption and ETL equivalence do not require root/Multipass; exhaustive transaction/fault coverage is mapped before privileged duplicates are removed.
- [x] 3.4  Move/split protocol/session/correlation and replay planner/executor/verifier matrices into owning crate unit/integration tests. Acceptance: P1/P2 replay scenarios retain only user or composition invariant and no longer invoke replay Cargo matrix as scenario step.
- [x] 3.5  Audit large CLI contract suite assertion-by-assertion. Keep argument mapping, rendering, exit mapping, and binary contract under CLI-owned integration; mark executable liveness for smoke, public feature behavior for acceptance, complete composition for E2E, and real cgroup/signal behavior for privileged coverage. Acceptance: direct binary tests remain application-boundary compliant and no lower Chronicle dev dependency is added.
- [x] 3.6 Split `loss_sampling_state_machine_covers_all_gate_a_boundaries` or equivalent portable state-machine proof from real `privileged_feasibility` kernel test. Acceptance: state machine runs rootlessly; remaining privileged feasibility assertion genuinely loads/attaches kernel programs.

## 4. Smoke and black-box acceptance suites

- [x] 4.1 Establish `tests/smoke/` coverage for shipped public executables and minimal supported public invocations: `chronicle --help`, basic usage error, list on an empty data directory, inspect on a minimal recording, replay dry-run, and public record startup or unsupported-build fail-fast behavior. Require `--version` only if the binary actually exposes it (the current binary does not; `--help` is the liveness probe). Remove the internal `chronicle internal etl` liveness case from Smoke (it belongs to the Integration internal-command contract, task 4.3). Acceptance: each test is black-box, minimal, bounded, does not perform full workflow or arbitrary correctness sleep, and proves only executable liveness/minimal public invocation; internal commands are not Smoke proof merely because they can be invoked as subprocesses; task 4.1 remains incomplete while `internal etl` remains in the Smoke suite.
- [x] 4.2 Establish black-box `tests/acceptance/` coverage for currently documented public user-facing features (record, inspect, replay, and any other feature with a documented public surface). ETL is NOT required as Acceptance unless ETL has a documented public command (see 4.3); internal ETL coverage currently under `tests/acceptance/test_etl.py` moves out of the Acceptance tree. Acceptance: each test maps to one documented user-facing feature contract, exercises it through supported public/external surfaces, imports no Chronicle implementation crate for assertions, and does not repeat parser/WAL/ETL/replay matrices (liveness belongs to smoke, binary/argument/rendering contracts belong to integration).
- [x] 4.3 Reclassify ALL internal ETL CLI command coverage (Acceptance AND Smoke) to a CLI-owned Integration owner, distinct from application ETL behavior: `tests/acceptance/test_etl.py`, the `chronicle internal etl` liveness case in `tests/smoke/test_smoke.py`, `acceptance:etl-process`, and `smoke:etl-minimal-start` converge on the CLI Integration contract (`integration:cli-contract`, or `integration:internal-etl-cli-contract` only if a separate ID gives real selection/ownership benefit) owned by the chronicle-cli boundary. The internal CLI contract covers command parsing/wiring, process execution, mapping CLI arguments to application behavior, exit-code mapping, JSON output/error rendering, stderr behavior, and fail-closed CLI behavior. Application ETL correctness (`crates/chronicle-application/tests/etl_contract.rs` → `integration:etl-contract`) remains separately owned by chronicle-application and is NOT deleted or merged by this reclassification. Add public ETL acceptance separately ONLY if ETL is later exposed as a documented public command. Acceptance: ledger/decisions explain every retained internal-command test; internal ETL command coverage has ONE CLI-owned Integration owner while application ETL correctness remains separately owned by the application ETL Integration contract; removing duplicate Smoke/Acceptance wrappers does NOT delete the application ETL contract; `acceptance:etl-process` no longer appears as acceptance and `smoke:etl-minimal-start` no longer appears as smoke under any gate; an internal command is never the sole evidence for a documented user-facing capability; merely invoking a subprocess does not make a test Smoke.
- [x] 4.4 CUTOVER shared HTTP workload/replay target from `tests/e2e/http_acceptance_driver.py` to `tests/support/http_driver.py` (or equivalent clearly-owned root black-box support); move its self-test to a support/tooling-appropriate location; update EVERY caller: `tests/support/process.py`, smoke tests, acceptance tests, E2E tests, CI (`.github/workflows/ci.yml`), validation commands, script/tooling self-tests, docs referencing the path, and catalog/ledger command metadata where applicable. Once all callers use the support-owned driver, the legacy E2E driver has zero active callers and is marked obsolete — eligible for physical deletion under task 11.4, NOT deleted by this task. Acceptance: no root black-box layer imports another root functional layer for shared infrastructure (the current `tests/support/process.py` → `tests/e2e/http_acceptance_driver.py` reference is a transitional violation removed by this task); support self-test is rootless and condition-based; the support file itself is not counted as E2E proof; support depends on nothing outside `tests/support/`; a repository search shows no active test, script, CI workflow, validation command, or documentation path references the old E2E-owned driver after migration; the legacy file may remain physically present until task 11.4 deletes it, preserving rollback.

## 5. Rootless and privileged end-to-end coverage

- [x] 5.1 Promote/adapt existing deterministic vertical slices into canonical rootless E2E from synthetic capture through WAL, ETL, canonical publication, replay, and verification. Acceptance: test proves stage consumability, identity/correlation continuity, expected operation survival/execution, and verification; no real eBPF, root, systemd, or Multipass requirement.
- [x] 5.2 Keep detailed downstream matrices in lower layers and reduce rootless E2E to important cross-stage invariants. Acceptance: review finds no exhaustive protocol/WAL/ETL/replay fault matrix in E2E.
- [ ] 5.3 Define and implement very small privileged E2E using real workload/eBPF capture then existing downstream path. Acceptance: unique privileged invariant is real capture feeds replayable pipeline and kernel/process/cgroup resources clean up; lower-layer downstream matrices are not rerun.
- [x] 5.4  Decide each existing full P1/P2 path as privileged capture integration, privileged acceptance, privileged E2E, or split coverage. Acceptance: real eBPF attach/capture, cgroup filtering, process attribution, bpftool cleanup, supported Ubuntu compatibility, and reboot remain privileged; portable replay/CLI/ETL/WAL assertions move out; cross-crate/process/system privileged black-box assertions target `tests/privileged/` ownership categories (capture/acceptance/e2e/recovery/resources) while crate-owned kernel contracts may remain in owning crate `tests/`.

## 6. Deterministic process and readiness infrastructure

- [x] 6.1 Inventory repeated `Popen`/`Command`, background server, port-file, poll, sleep, signal, kill, wait, trap, and cleanup patterns across Rust, Python, and shell tests. Acceptance: identify at least two consumers before proposing each shared helper and mark one-off support local.
- [x] 6.2 Reuse `scripts/run-with-timeout.sh`, acceptance `wait_until`, and specialized recorder readiness; introduce minimum language-local shared helpers for repeated Chronicle/Recorder process ownership, workload server readiness, condition waits, evidence collection, cleanup, and timeout. Acceptance: no new framework/dependency/shared workspace crate without separately justified architecture update; root black-box layers (smoke/acceptance/E2E) share infrastructure only through `tests/support/` and never depend on each other to reach shared helpers; task 6.2 remains incomplete while the `tests/support/process.py` → `tests/e2e/http_acceptance_driver.py` dependency exists (removed by task 4.4).
- [x] 6.3  Migrate black-box tests from arbitrary synchronization sleeps to observable bounded conditions; preserve explicit elapsed-time behavior only for age policies followed by convergence polling. Acceptance: static/meta-tests reject undocumented correctness sleeps and unbounded polling while fixture sleeper workloads remain allowed.
- [x] 6.4 Guarantee deterministic cleanup on pass, failure, signal, and timeout. Acceptance: focused lifecycle tests prove no leaked process, port, cgroup, mount/loop, control file, or stale recorder ownership for helpers/scenarios they own.
- [x] 6.5 Preserve recorder readiness specialization. Acceptance: fresh systemd activation, stale-owner rejection, terminal failure, status command deadline, transition logging, and bounded diagnostics remain covered and generic waits do not reinterpret recorder state.

## 7. Privileged preflight and environment/executor separation

- [x] 7.1 Define versioned machine-readable privileged preflight schema and stable outcome/exit mapping for `supported`, `unsupported_environment`, and `infrastructure_error`, reusing doctor probe vocabulary where practical. Acceptance: schema records OS, architecture, Ubuntu/kernel, capabilities/root, cgroup v2, BTF, bpffs, required hooks/tooling, executor readiness, stable codes, messages, and remediation.
- [x] 7.2 Extract environment preparation from test behavior: VM ensure/start/readiness, bootstrap, immutable source transfer, remote execution, reboot handoff, artifact transfer, and evidence sealing remain under dedicated privileged orchestration such as `scripts/privileged/`; classified test assertions live under owning test paths. Acceptance: executor takes selected test IDs and contains no WAL/ETL/CLI/replay correctness assertions.
- [x] 7.3 Distinguish preflight unsupported, VM/bootstrap/transfer infrastructure error, timeout, `not_checked`, and supported-environment product failure in runner/report/evidence. Acceptance: rootless runner tests cover each state; unsupported/infrastructure never appears as product regression and never satisfies/reuses gate evidence.
- [x] 7.4 Preserve supported Ubuntu 24.04/kernel/BTF/cgroup/capability compatibility, content-addressed fingerprinting, source provenance, same-run source stability, complete manifest, scenario attribution, timeout records, and cleanup evidence. Acceptance: previous compatible evidence remains reusable only when new classified coverage/schema requirements are met; incomplete legacy evidence fails closed.

## 8. Remove gate-owned and privileged duplication

- [ ] 8.1 Remove portable Cargo test invocations from P1/P2 scenario bodies after their independent classified prerequisites pass: WAL/ingest, replay, cgroup decision logic, CLI signal where portable aspects exist, format, and workspace checks. Acceptance: gate still selects each prerequisite once and retained evidence records its result separately.
- [ ] 8.2 Split P1 `capture-basic` into supported-environment preflight, minimal real capture assertion, user acceptance/E2E selection, and lower-layer prerequisites. Acceptance: privileged portion no longer acts as environment setup + full test framework + repository CI suite.
- [ ] 8.3 Split P1/P2 WAL-recovery, replay, CLI-compatibility, user-intent, and resource-cleanup scenarios by functional purpose and privilege need. Acceptance: each remaining privileged assertion names real environment invariant; portable pieces run rootlessly.
- [ ] 8.4 Reclassify P2 incremental ETL, checkpoint kill/restart, quota pressure, retention interruption, corruption quarantine, recorder readiness, and reboot recovery. Acceptance: deterministic copied-WAL corruption and exhaustive ETL/checkpoint logic become rootless; real process interruption/systemd, loop/mount quota resources, kernel cleanup, and reboot remain privileged only where assertion requires them.
- [ ] 8.5 CUTOVER gate obligations from duplicate/gate-owned scenario assertions to replacement classified test IDs: switch gate selection, prove selector equivalence, remove legacy wrappers from ACTIVE selection, prove no active gate depends on obsolete scenario-owned assertions, and mark duplicates/wrappers obsolete (physical deletion is tasks 11.1/11.2). Acceptance: gate selection references replacement IDs only; selector equivalence is proven with rollback path intact; legacy duplicates/wrappers remain physically present until Section 11 cleanup; no active gate depends on obsolete scenario-owned assertions.

## 9. Validation, CI, evidence, and documentation

- [x] 9.1  Update `validation/groups.toml`/catalog and `scripts/validation.py` selection to report functional layer, environment requirement, owner, gate reason, selected/skipped tests, and no-coverage-loss decisions. Acceptance: docs-only, ETL-only, WAL-only, eBPF, CLI, helper, acceptance, E2E, privileged, and unknown-path fixtures select expected smallest valid set with conservative unknown behavior; selection machinery reads layer, environment, owner, command, status, and gate membership from catalog metadata (final catalog reconciliation with migrated IDs is task 9.7; the catalog remains classification/selector metadata, not a file-manifest database).
- [x] 9.2  Update `scripts/validate.sh` while preserving public commands. Acceptance: `fast` runs fmt, warnings-denied Clippy, architecture/source/OpenSpec checks, unit, and cheap integration; `targeted` is dependency-aware; P1/P2 select required hierarchy; release requires complete hierarchy plus compatible supported-platform evidence. OpenSpec remains structural proof only; tooling self-tests remain under `scripts/tests/` and are never counted as product acceptance coverage.
- [ ] 9.3 Update CI and every validation/tooling caller when classified test IDs or support paths migrate. Acceptance: ordinary CI runs portable fast/tooling/smoke/rootless coverage intended for hosted runner; privileged jobs/evidence remain explicit supported-platform concern; artifact retention/cache separation stays compliant; CI references final classified IDs/paths — current direct invocations of `tests/acceptance/test_etl.py` and `tests/e2e/http_acceptance_driver.py` in `.github/workflows/ci.yml` are updated when tasks 4.3/4.4 land; CI MUST execute tests or support self-tests and MUST NOT invoke a support executable as though it were a test (`tests/e2e/http_acceptance_driver.py` is never invoked directly by CI after migration; its self-test/tooling test is invoked instead).
- [ ] 9.4 Update evidence/report compatibility for classified tests and preflight/infrastructure outcomes. Acceptance: P1/P2/release reports identify executed/reused functional layers and environment-qualified tests; timeout, unsupported, infrastructure, failed, and `not_checked` candidates cannot be reused.
- [x] 9.5 Document the TARGET test architecture in detailed testing docs and `AGENTS.md`: classification rules, lowest-cost conclusive-proof rule, target directory responsibility contract, privilege dimension, preflight outcomes, helper ownership, gate semantics, and crate-boundary constraints. Acceptance: guidance states the target rules above (final migration-outcome reconciliation is task 9.8).
- [x] 9.6 Remove stale P1/P2 test-category terminology and update developer/operations docs without changing milestone feature requirements. Acceptance: searches find no claim that P1/P2 directories define test architecture or privileged acceptance owns portable correctness.
- [ ] 9.7 FINAL catalog/gate reconciliation after ALL migrations affecting classified test ownership or execution complete (relevant tasks 3.x–8.x, including 4.4 HTTP support migration, 5.3 privileged E2E, 6.2 support boundary completion, 7.x privileged executor changes, and 8.x gate-owned scenario decomposition): `acceptance:etl-process` → removed/reclassified from Acceptance; `smoke:etl-minimal-start` → removed/reclassified from Smoke; internal ETL CLI seam → CLI-owned Integration classification (`integration:cli-contract` or `integration:internal-etl-cli-contract`); `integration:etl-contract` → remains application ETL behavior owned by chronicle-application (the catalog MUST NOT collapse these distinct contracts into one test to reduce IDs); catalog IDs, owners, layers, environments, commands, gate mappings, and status match final target architecture ownership. Acceptance: catalog and gate-coverage reference only final classified IDs; no legacy ID survives without a caller or migration record; responsibility boundaries are preserved (application ETL ≠ internal ETL CLI); the internal ETL seam is not counted as Smoke or Acceptance merely because it is executable through the CLI binary; after 9.7 completes, the ACTIVE catalog and gate configuration already represent the final architecture — task 11.9 may organize migration bookkeeping only and MUST NOT change active test IDs, classifications, commands, ownership, or gate mappings established here.
- [ ] 9.8 Reconcile final testing documentation and `AGENTS.md` with the completed migration outcome. Acceptance: docs match actual final filesystem locations, final test catalog IDs, final Smoke/Acceptance/Integration classifications, final internal-command ownership (application ETL → `integration:etl-contract` owned by chronicle-application; internal ETL CLI seam → CLI-owned Integration contract owned by chronicle-cli; application ETL ≠ internal ETL CLI), final `tests/support/` ownership (HTTP driver under `tests/support/`), final privileged executor structure, final P1/P2 selector behavior, final cleanup result, and final CI invocation model; documentation does not describe transitional paths as final architecture.

## 10. Verification and completion evidence

Completion dependency order: tasks 4.1–4.4 → 5.3 / 6.2 / 7.x / 8.x → 9.3 (final CI caller reconciliation), 9.4 (evidence/report compatibility), 9.7 (FINAL active catalog/gate reconciliation) → section 11.1–11.8 (physical cleanup, gated on 9.7) → 10.3 (final pre/post coverage + runtime comparison — MUST run while all migration evidence required for the comparison is still retained) → 11.9 (migration bookkeeping cleanup — ONLY after 10.3 completes and its comparison evidence is retained) → 11.10 (no-dead-infrastructure invariant, AFTER all cleanup including 11.9) → 9.8 (final docs/AGENTS reconciliation — BEFORE retained final evidence, because it changes the source tree) → 10.4 (fresh supported Linux P1/P2 proof) → 10.5 (final release validation on the final reconciled checkout) → 10.7 (final non-goals confirmation) → 10.6 (final OpenSpec reconciliation). Section 11 cleanup MUST precede fresh final supported-platform proof so retained evidence reflects the post-cleanup, post-bookkeeping, post-doc checkout; the change MUST NOT be declared complete with cleanup pending, docs unreconciled, or final supported evidence generated before cleanup, bookkeeping cleanup, and documentation.

- [x] 10.1 Run bounded catalog/meta-tests, architecture tests, validation-tooling tests, process/readiness helper tests, every affected crate unit/integration suite, smoke, black-box acceptance, and rootless E2E. Acceptance: all pass; no implementation task is checked from OpenSpec validation alone.
- [x] 10.2 Run bounded `./scripts/validate.sh fast` and representative targeted fixtures/real changed-path selections. Acceptance: selected/skipped explanations match responsibility/environment model and no unrelated privileged gate is selected without stated milestone reason.
- [ ] 10.3 Compare pre/post migration gate coverage matrix and runtime command trace. Inputs: baseline-to-final obligation mapping, migration ledger dispositions, legacy-command mapping, duplicate-reduction accounting, selector-equivalence evidence, and runtime command comparison — consumed from `baseline.json`, `migration-ledger.toml`, `test-catalog.toml`, and `gate-coverage.toml` while still intact. Acceptance: every original required product/environment invariant has equal or stronger replacement proof, portable checks run outside privileged executor, high-level duplicate count/runtime decreases without weakening evidence, and all final comparison evidence is retained BEFORE task 11.9 runs (task 11.9 must never destroy evidence required to prove or audit this comparison).
- [ ] 10.4 On supported Linux per task responsibility, run required fresh/compatible P1/P2 scenarios and retain machine-readable evidence. Acceptance: only tasks requiring privileged proof are checked from supported-environment retained evidence; unsupported/infrastructure results remain blockers, not product passes/failures.
- [ ] 10.5 Run FINAL release validation on the final reconciled checkout (post-cleanup, post-migration-bookkeeping, post-documentation): release-path dry-run/fixture compatibility and, when release evidence is required, clean-checkout bounded release validation. Acceptance: complete classified hierarchy and supported-platform evidence are executed or compatibly reused, current checkout stays identifiable/unchanged, and historical commit equality is not required; task 10.5 validates the SAME final source tree represented by task 10.4 supported-platform evidence and task 9.8 final documentation reconciliation — a pre-9.8 checkout is not sufficient; earlier release-path runs are progress evidence only.
- [ ] 10.6 Validate OpenSpec strictly, reconcile implementation against proposal/design/spec/tasks, and update `graphify` after code changes. Acceptance: `openspec validate reorganize-test-suite-by-responsibility-and-runtime-boundary --strict --no-interactive` passes; graph update succeeds; only evidence-backed task boxes are checked; final reconciliation runs LAST, after section 11 cleanup, final coverage comparison (10.3), the no-dead-infrastructure invariant (11.10), final docs/AGENTS reconciliation (9.8), fresh supported proof (10.4), release validation (10.5), and final non-goal confirmation (10.7) — earlier strict OpenSpec validations are progress checks only; task 10.6 is complete only after final documentation reconciliation, fresh supported-platform evidence, release validation, and final non-goal confirmation all pass; the change cannot be declared complete with cleanup pending, docs unreconciled, or final supported evidence generated before cleanup and documentation.
- [ ] 10.7 Confirm FINAL non-goals immediately before final OpenSpec reconciliation (10.6) and after fresh supported proof (10.4) and release validation (10.5). Acceptance: no production behavior/API/format, CLI design, protocol, eBPF semantics, P1/P2 feature requirement, crate count, or unapproved dependency/framework change occurred; any such delta blocks completion and requires new OpenSpec scope; earlier scope reviews are progress evidence only — conclusive non-goal confirmation is possible only after tasks 5.3, 7.x, 8.x, and 11.x are implemented.

## 11. Remove obsolete test code and migration scaffolding

Section 11 performs PHYSICAL deletion and dead-code cleanup. Migration tasks (4.4, 8.5, and other 3.x–8.x tasks) perform CUTOVER: create replacement → prove it → switch callers/selectors/catalog ownership → prove zero active dependency on the legacy implementation → mark the legacy item obsolete. Legacy implementations may physically exist between cutover and cleanup; physical deletion never occurs while active selectors/catalog/CI reference them.

All cleanup below is gated: it runs ONLY after replacement test IDs exist and pass, gate coverage mapping is complete (task 1.3), no-coverage-loss structural checks pass (task 2.2), final selector behavior is implemented and proven (tasks 9.1/9.2), evidence schema compatibility required for safe cleanup is implemented (task 9.4), and final ACTIVE catalog/gate reconciliation is complete (task 9.7) so physical deletion never happens while the active catalog still points at legacy test IDs or obsolete commands. Cleanup MUST occur BEFORE fresh final supported-platform proof: task 10.4 runs only after section 11 cleanup and the no-dead-infrastructure invariant (11.10), so retained evidence never refers to a pre-cleanup checkout. Cleanup must never precede replacement proof.

- [ ] 11.1 PHYSICALLY remove duplicate product assertions marked obsolete by task 8.5 cutover: duplicate WAL checksum/corruption matrices, ETL idempotence/checkpoint/corruption matrices, replay algorithm matrices, CLI parsing/rendering checks, portable cgroup decision logic, and repository fmt/workspace checks embedded in acceptance scenarios (across root suites and privileged profiles). Acceptance: replacement test IDs are recorded in the ledger, replacement tests pass, gate obligations reference replacements, no required invariant disappears, and privileged runtime no longer reruns portable correctness.
- [ ] 11.2 PHYSICALLY remove obsolete P1/P2 scenario wrappers marked obsolete by task 8.5 cutover. Wrappers whose only role is running another classified test or carrying milestone-oriented test ownership are deleted; wrappers still required for genuine environment orchestration are retained until a replacement exists. Acceptance: no wrapper remains solely for historical P1/P2 naming, no compatibility shim remains without a real caller, and P1/P2 are represented by selector metadata rather than directory ownership.
- [ ] 11.3 Remove obsolete acceptance runner logic as product assertions move out of `scripts/acceptance`: unused scenario state fields, obsolete result variables, obsolete matrix status fields, obsolete command dispatch paths, unused scenario-specific setup, duplicate lifecycle helpers, and dead report fields that no longer represent classified tests (legacy concepts such as `WAL_MATRIX_RESULT`, `INGEST_MATRIX_RESULT`, `REPLAY_MATRIX_RESULT`, `FMT_RESULT`, `WORKSPACE_CHECK_RESULT` disappear from privileged product evidence when independently selected as portable prerequisites). Acceptance: reports reference classified test IDs/results rather than old embedded matrix variables; dead fields are removed only after evidence compatibility migration completes; no legacy field remains merely because old scripts once produced it.
- [ ] 11.4 PHYSICALLY remove obsolete HTTP/E2E support placement after task 4.4 cutover proves zero active callers: delete `tests/e2e/http_acceptance_driver.py` and any superseded self-test path. Acceptance: no acceptance/support code imports E2E infrastructure, shared support has one clear owner, old files have no callers before deletion, and no duplicate HTTP driver implementation exists.
- [ ] 11.5 Remove speculative or empty directories: empty migration directories, empty placeholder privileged directories, unused test package markers where unnecessary, obsolete compatibility directories, dead fixture folders, and stale temporary migration structures that have no real caller/test ownership. Acceptance: in the final checkout, no empty target-directory scaffolding remains or is introduced solely to resemble the design tree; every remaining directory has real test ownership.
- [ ] 11.6 Remove dead fixtures after migration: fixtures that duplicate another canonical fixture, were used only by deleted legacy scenarios, encode obsolete schemas, or are no longer referenced by any test/catalog/gate obligation. Acceptance: every retained fixture has at least one named owner/consumer; no deleted fixture is still referenced by catalog/ledger/tests/docs; no production-compatible historical fixture is removed without explicit compatibility review.
- [ ] 11.7 Remove dead helper functions across `tests/support/**`, `scripts/acceptance/**`, `scripts/privileged/**`, `scripts/tests/**`, and crate-local test support: duplicate polling helpers, duplicate subprocess wrappers, duplicate temp-dir helpers, dead readiness functions, dead assertion utilities, and one-off general helpers that no longer have multiple consumers. Acceptance: each shared helper has a clear owner and real consumers, one-off support stays local, and no helper exists only because migration once needed it.
- [ ] 11.8 Remove tracked generated artifacts from test architecture: `__pycache__/`, `*.pyc`, temporary evidence, temporary test output, and local VM artifacts where present; update ignore rules only if needed. Acceptance: generated artifacts are not part of test architecture or evidence provenance; hygiene is treated as cleanup, not a product-test layer.
- [ ] 11.9 Clean up temporary migration bookkeeping and historical organization ONLY — 11.9 MUST NOT change final ACTIVE test architecture metadata established by task 9.7. It may remove obsolete migration-ledger compatibility rows, abandoned temporary bookkeeping, transitional migration-only annotations, and stale non-active references when ledger policy allows archival. It MUST NOT change final active test IDs, functional classifications, commands, ownership, or gate mappings. Preserve baseline snapshots (`baseline.json`), migration decisions (`decisions.md`), historical compatibility evidence, and intentionally retained audit artifacts. Acceptance: active catalog/gate configuration is unchanged by 11.9; deleted items are temporary migration bookkeeping/history organization, never audit records; historical evidence stays distinct from active runtime configuration; task 11.9 runs only after task 10.3 final comparison is complete and its evidence retained, and MUST NOT delete or rewrite evidence required to reproduce or audit the final 10.3 coverage/runtime comparison.
- [ ] 11.10 Final no-dead-testing-infrastructure completion invariant. Acceptance: after migration, every retained product test, helper, fixture, scenario wrapper, validation entry, and privileged orchestration file has a current owner and caller or a documented historical/audit purpose; dead compatibility wrappers, duplicate assertions, unused fixtures/helpers, generated files, empty scaffolding, and obsolete gate-owned test implementations are removed before the change is considered complete; cleanup is subordinate to no-coverage-loss and never precedes replacement proof and gate mapping; the invariant is verified AFTER section 11.1–11.9 cleanup and BEFORE fresh supported-platform proof (10.4).
