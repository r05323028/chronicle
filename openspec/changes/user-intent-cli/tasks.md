## 1. Recording identity and data-directory foundation

- [x] 1.1 Add `RecordingId` CLI formatting/parsing helpers in `chronicle-common`: `to_cli_string()` producing `rec_<uuid>`, and a parser accepting `rec_<full-uuid>` or bare UUID, rejecting prefixes; unit tests for display, both accepted forms, and prefix rejection. Acceptance: `cargo test -p chronicle-common` passes; no storage-format change.
- [x] 1.2 Implement `resolve_data_dir` in `chronicle-application`: explicit `--data-dir` > configured `AppConfig.data_dir` > `CHRONICLE_DATA_DIR` > Linux/macOS default; other platforms require explicit/configured/env path. Inject env/home lookup in tests—do not mutate process-global `HOME`/`XDG_DATA_HOME` in parallel. Reject symlinked roots/children and create lazily/private. Acceptance: precedence/default/unsupported/symlink tests pass without touching real home dirs.
- [x] 1.3 Add `--data-dir` global option to `chronicle-cli`; keep legacy `--root` behavior untouched on compatibility paths. Acceptance: `chronicle --data-dir /tmp/x list` uses `/tmp/x`; legacy `inspect S --root R` still uses `R`.
- [x] 1.4 Define advisory catalog v1 schema with stable statuses (`in_progress`, `recoverable`, `published`, `failed`, `inconsistent`) and atomic helpers reusing private JSON writes. Enforce 16 MiB/10,000-entry bounds, immediate-child/no-follow scans, regular-file/ID-directory checks, and canonical/WAL/catalog precedence. Acceptance: atomic write, non-v1, oversized, symlink/path escape, duplicate ID/name, malformed fallback, and contradiction tests.
- [x] 1.5 Extract one exact domain-lock opener/resolver used by public record and `RecorderLease`: add optional `AppConfig.domain_lock_root`, default root = data dir, path = `<root>/.chronicle-domain.lock`; never infer flock equivalence from device identity or `state_is_owned`. Return held guard/token required by catalog/sidecar mutation. Acceptance: same exact configured path conflicts across public/daemon modes; same configured data root/domain with mismatched lock path is rejected; different paths on same device are never claimed equivalent; unsupported multi-domain-on-one-filesystem case is documented; unsafe path rejected.

## 2. Recording catalog and resolution

- [x] 2.1 Add read-only `list_summaries(limit)` to `FilesystemSessionStore`, capped at 10,000, returning id/started/ended/operation counts without payload hydration and rejecting unsafe/non-regular entries. Acceptance: existing tests pass; bound/symlink tests and no-mutation snapshot pass.
- [x] 2.2 Implement bounded catalog reconciliation: build read-only in-memory view from sidecars, recovery-authoritative WAL metadata, and session summaries; canonical facts win, WAL determines recoverability, contradictions become `inconsistent`; never ETL/re-publish. Persist rebuild only while exact domain lock is held; reads while daemon owns lock remain non-mutating. Acceptance: empty-catalog adoption, precedence/contradiction, owned-domain read, and no-publication tests.
- [x] 2.5 Implement private v1 `recording-intent.json` under `recordings/<bare-uuid>/`, max 4 KiB, persisted after preflight/scope creation but before capture attachment under exact domain lock. Enforce no-follow/path confinement and ID-directory match. Acceptance: schema/version/oversize/symlink tests; catalog-loss recovery preserves names/timestamps; write without guard rejected.
- [x] 2.3 Implement `resolve_recording(data_dir, ref)` for `latest` / exact ID / name, with `latest` = newest published by canonical started_at, tie-break lexicographic recording ID; unresolved refs error with exit 3. Acceptance: unit tests for latest ordering, tie-break, ID, name, and unresolved.
- [x] 2.4 Enforce names as exact UTF-8 1–128 bytes with no control characters; reject `latest`, case-sensitive `rec_*`, and collisions under lock before capture. Human rendering escapes untrusted references. Acceptance: empty/oversize/newline/ANSI/reserved/collision tests exit 2/3 with no capture or output injection.

## 3. CLI hierarchy and dispatch split

- [x] 3.1 Add public `record`, `replay`, `list`, `inspect`, `doctor` plus hidden `internal`/legacy parsing and thin dispatch/rendering. Split files only where it reduces complexity; no mandatory module refactor. Acceptance: existing CLI contracts pass; public help omits internal flags/mechanics.
- [x] 3.2 Wire global `--format human|json` and `--data-dir` through all five public commands. Acceptance: `--format json list`, `inspect`, `doctor` produce one atomic JSON object each.
- [x] 3.3 Add `--duration` (e.g. `5m`, `2h`, default 600s/max 3600s), `--name`, and mutually exclusive `--retry RECORDING` to `record`; retry conflicts with command/PID/cgroup/name/duration capture options. Acceptance: parse/conflict tests; over-max exits 2.
- [x] 3.4 Implement `list` command rendering: human table (ID NAME CREATED DURATION SESSIONS OPERATIONS STATUS, newest first) and versioned JSON with the same fields; SESSIONS = published session count for the recording (one in 0.1.x), OPERATIONS = total canonical operation count; empty state exit 0 with hint. Acceptance: contract tests for populated, empty, and JSON forms.

## 4. Command-mode record orchestration

- [x] 4.1 Add Linux supervised-scope helper: create/move/TERM/wait/KILL/wait-empty/destroy plus access-separation preflight and stable identity. Hard bounds: TERM 5s, KILL wait 5s, absolute cleanup 15s; process-group fallback forbidden; non-Linux typed unsupported. Acceptance: fake-clock transition faults, resistant child completes by deadline or exits 3 with orphan evidence, access rejection, identity change, populated cleanup.
- [x] 4.2 Implement `record_command`: non-mutating preflight first; exact domain lock + revalidation; scope creation before durable allocation; caller-created ID/metadata passed into lower-level `record_production` path; parent-pipe bootstrap; attach-before-direct-exec; trusted invoking UID/GID, all capability sets cleared, `no_new_privs`, descriptors closed/CLOEXEC, signals reset; bounded cleanup/finalization/publication; lock released last. Human child stdio may pass through; JSON connects both streams to platform null sink before exec and creates no child-output file/buffer. Attach readiness hard-deadlines at 30s. Acceptance: Linux integration proves no target marker before attach, timeout leaves no recording/target, in-scope descendant capture, exact ID continuity, factual app exit, typed exec failure (not 127), credentials/FD state, arbitrary-volume noisy child with atomic JSON and no output artifact/buffer, cleanup deadlines at injected transitions, and full lock lifetime.
- [x] 4.3 Implement `record --retry RECORDING`: only recovery-authoritative `recoverable` entries; exact lock; no target/capture; idempotent recovery/ETL/publication/catalog completion for same ID. Acceptance: faults before/after ETL, publication, and catalog update retry without recapture or duplicate session; non-recoverable/owned-domain cases fail safely.
- [x] 4.4 Wire `record --pid PID` and `record --cgroup PATH` to existing `record_live_ebpf` (attach, bounded run, detach, never terminate workload) followed by the same internal WAL → ETL → publish → catalog workflow as command mode, acquiring and holding the same exact configured data-domain lock for the entire capture→publish transaction; mutually exclusive with command mode (clap conflict → exit 2); preserve shared-scope acknowledgement and PID direct-TGID safety; only hidden legacy `record --source ebpf --wal-dir ...` retains no implicit ETL. Acceptance: existing production-record CLI tests, new mutual-exclusion test, and a concurrent record/name-claim test asserting the second process fails cleanly while the first holds the lock.

## 5. Command-mode replay orchestration

- [x] 5.1 Implement target-independent replay planning before spawn, then supervised scope only for listener-derived target. Reuse hardening, JSON null-sink child output, 30s readiness, and 5s/5s/15s cleanup bounds. Linux/access separation required; no process-group fallback; `--target` workloads never terminated. Acceptance: predictable denial leaves target marker absent; no-listener readiness resolves ≤30s and command cleanup finishes ≤45s total with zero replay traffic; resistant/noisy child respects bounds; unavailable scope actionable.
- [x] 5.2 Implement stable listener discovery from `(pid,start_time,fd,socket_inode)` joined to `/proc/net/tcp{,6}`; exact loopback protocol/port match, immediate revalidation, ≤100ms polling, ≤5 unstable-snapshot retries within 30s, no namespace-table-only evidence or host scan. Preserve exact IPv4/IPv6 origin. Acceptance: unique/absent/ambiguous/non-loopback, membership/FD race retry/fail, `/proc/<pid>/net` insufficiency, IPv6-only success, and same-port opposite-family decoy never selected.
- [x] 5.3 Build inferred replay options from unique listener and call existing `replay_session_with_plan`: inferred execution intent, loopback target, host match, read-effect authorization; writes and other effects stay explicit; no replay-crate execution-path change. Acceptance: replay crate tests unchanged and passing; integration test proves POST is denied without write authorization in command mode.
- [x] 5.4 Keep `replay RECORDING --target URL` mode mutually exclusive with command mode, requiring the full existing explicit authorization gates (execution, target, host, effects) and forbidding recorded destinations. Acceptance: existing `replay_arguments` contract tests pass; new conflict test exits 2 with zero traffic.

## 6. Inspect and doctor improvements

- [x] 6.1 Update `inspect <RECORDING>` to resolve via `resolve_recording` and default data dir; keep legacy `inspect SESSION --root ROOT` hidden compat; extend JSON with recording identity; human output keeps prioritized metadata/duration/counts/protocols/endpoints/loss warnings/replay eligibility. Acceptance: inspect-by-id, inspect-latest, unknown-exit-3, and legacy-form tests pass.
- [x] 6.2 Add data-directory probe reporting source and existing/prospective status without creation. Use `not_checked` when ancestor metadata cannot prove creation/private-mode support; never claim a destructive probe occurred. Acceptance: existing/nonexistent/uncertain/symlink tests plus no persistent artifact/capture/repair/replay traffic.
- [x] 6.3 Ensure `doctor` renders remediation strings for unsupported OS, kernel, cgroup v2, BTF, capabilities, data-dir access, and replay configuration failures. Acceptance: probe tests assert non-empty remediation per failing case.

## 7. Compatibility entrypoints and deprecation

- [ ] 7.1 Add explicit internal namespace `chronicle internal recorder`, `internal recorder-status`, `internal etl`, `internal record-fixture`; re-register top-level `recorder`, `recorder-status`, `etl`, legacy `record --source fixture|ebpf ...`, legacy `replay SESSION --root ...`, and legacy `inspect SESSION --root` as hidden deprecated aliases routing to the same application services. Acceptance: every existing CLI contract test passes unchanged; `--help` hides them; `internal etl` works.
- [ ] 7.2 Add deprecation warnings: human stderr; JSON success one v1 warning stderr + byte-stable stdout; JSON failure one normal error only. `invocation` uses fixed form ID and `replacement` safe syntax template—never raw argv, URL userinfo, or child command. Acceptance: secret-bearing argv regression proves no leak.
- [ ] 7.3 Add new-syntax hints to legacy-form errors. Acceptance: invalid legacy invocation error text names the equivalent new command.
- [ ] 7.4 Update the systemd unit `docs/systemd/chronicle-recorder.service` invocation to `chronicle --format json internal recorder --config /etc/chronicle/recorder.toml` and document the deprecation schedule (removal 0.2). Acceptance: unit file references the internal form; docs state the 0.2 removal boundary.

## 8. Output and exit contracts

- [ ] 8.1 Implement record human summary per normative spec example and public record v1 JSON (canonical ID, nullable name, stable status/reason, duration ms, operation/drop totals, existing categorized counters, structured nullable child result). Try block uses `COMMAND...`, never raw argv. Acceptance: v1 schema/golden/nullability/enum/control-escape tests.
- [ ] 8.2 Implement replay human PASS/FAIL summary (`✓ passed` / `✗ failed`, final `Replay passed.`/`Replay failed.`) on top of existing renderers. Acceptance: golden-output tests for pass and fail.
- [ ] 8.3 Verify 0/2/3/4/5/6 across new/legacy commands; post-exec app exit/signal factual and not forwarded; bootstrap/hardening/exec failure typed 3/4; inspect/replay/retry resolution exit 3; list storage error exit 3. Acceptance: exit matrix.

## 9. Documentation

- [ ] 9.1 Rewrite `docs/operations.md` quick-start around `chronicle record -- ...`, `chronicle list`, `chronicle inspect latest`, `chronicle replay latest -- ...`; keep a legacy-syntax migration appendix. Acceptance: doc examples execute against the new CLI.
- [ ] 9.2 Update `docs/replay-safety.md` with the inference-vs-explicit table and the deny-by-default statement for writes/authentication/publication/unknown. Acceptance: table matches spec scenarios.
- [ ] 9.3 Update `docs/continuous-recorder.md` and `docs/continuous-recorder-runbook.md` for the hidden daemon form; add data-directory and recording-identity sections to `docs/architecture.md` or a new doc. Acceptance: no doc references removed public flags as normal usage.
- [ ] 9.4 Note Docker/Kubernetes packaging as future follow-up only in docs; no implementation. Acceptance: docs mention as future work, no code.
- [ ] 9.5 Update README and `chronicle --help`/subcommand help text: replace the fixture-oriented CLI description with the intent-oriented quick start (`record --`, `list`, `inspect latest`, `replay latest --`). Acceptance: README and help text describe the new surface; no primary-path reference to fixture/`--wal-dir` remains.

## 10. Tests: unit, integration, CLI, rootless, privileged

- [ ] 10.1 Unit tests for identity helpers, data-dir resolution, catalog read/write/rebuild, name rules, listener-discovery predicates, and scope helper fault paths. Acceptance: `cargo test --workspace` passes on macOS (non-Linux paths).
- [ ] 10.2 Rootless CLI contract tests for all five commands, JSON atomicity, exit codes, deprecation warnings, and legacy forms. Acceptance: `crates/chronicle-cli/tests/cli_contract.rs` extended and passing.
- [ ] 10.3 Linux rootless replay command test: exercise happy discovery only when access-separated delegated cgroup exists; otherwise assert actionable preflight and zero target execution/traffic. Negative fallback is not listener-discovery evidence; task 10.5 privileged scenario remains required happy-path proof.
- [ ] 10.4 Portable rootless integration test for explicit `--target` replay mode (loopback server already running), asserting full authorization gates and no recorded-destination contact; runs on macOS and non-Linux CI without cgroups or `/proc` ownership. Acceptance: portable test passes in CI on macOS.
- [ ] 10.5 Privileged supported-Linux acceptance: release-built `linux-ebpf` binary; real record command/descendants, Ctrl+C cleanup, PID/cgroup attach, retry after injected publication failure without recapture, replay IPv4 and IPv6 PASS/FAIL, denied-write target-not-started, original destination untouched, credential/cgroup access separation, and no owned scope/process/eBPF leak after normal/fault exits. Retain machine-readable evidence per existing rules.
- [ ] 10.6 Keep existing P1/P2 acceptance scenarios green against the new surface after migrating their invocations; run targeted validation group. Acceptance: `validate.sh fast` equivalent passes with migrated scripts.

## 11. Migration tests for old CLI syntax

- [ ] 11.1 Add a dedicated compatibility scenario that runs each legacy form (`etl --wal-dir`, `record --source fixture`, `record --source ebpf`, legacy `replay`, legacy `inspect`) and asserts identical behavior plus deprecation warning. Acceptance: scenario passes on 0.1.x surface.
- [ ] 11.2 Add a migration test asserting new-syntax error hints appear for invalid legacy invocations. Acceptance: test passes.
- [ ] 11.3 Grep-audit task: ensure docs, systemd unit, acceptance scripts, and tests no longer present legacy syntax as the primary path (migration complete); legacy forms remain only as documented compat. Acceptance: audit report shows zero primary-path legacy invocations.

## 12. Release and rollback

- [ ] 12.1 Confirm no existing authoritative WAL/canonical/session-manifest format changes while documenting additive catalog/sidecar v1. Rollback reverts coordinated crates; previous release can inspect explicit `<data>` root/process explicit WAL but ignores names/latest/sidecars/retry. Acceptance: controlled previous-release binary fixture test states supported directions; no claim of CLI-only rollback.
- [ ] 12.2 Update release notes with deprecation schedule and migration guide pointer. Acceptance: notes present in release doc.
- [ ] 12.3 Run strict OpenSpec validation, bounded fast/targeted tests, then required privileged P1/P2 scenarios on supported Linux. Acceptance: OpenSpec strict passes structurally; runtime tasks are checked only from their explicit automated/retained privileged evidence, never OpenSpec validation alone.
