# Implementation Tasks

Tasks appear in dependency order. Each checkbox names owning area and required proof. OpenSpec validation proves artifacts only; privileged task completion requires retained supported-Linux evidence explicitly named by that task.

## 1. Recorder State Primitives

**Entry:** archived P1 production pipeline and current rootless suites pass. **Exit:** configuration, lease, lifecycle state, and metadata primitives are deterministic and fault-tested without starting capture.

- [ ] 1.1 `[chronicle-application]` Define recorder configuration v1 for one cgroup scope/state root, epoch/segment/quota/retention/ETL/store/shutdown bounds, and one quota/minimum-free reserve per filesystem domain; normalize stable digest; reject unknown fields/version, contradictory bounds, unsafe paths, missing explicit retention mode, and scope/state identity mismatch. **Proof:** round-trip, non-v1, invalid-bound, filesystem-domain, digest-stability, and safe-error tests.
- [ ] 1.2 `[chronicle-application]` Add OS-released exclusive recorder-state lease separate from per-recording WAL lock; reject concurrent owner before mutation/attach; never use PID existence as authority. Depends on 1.1. **Proof:** subprocess concurrent-owner, crash-release, PID-reuse, and no-mutation tests.
- [ ] 1.3 `[chronicle-application]` Define lifecycle state machine `starting -> recovering -> running -> draining -> stopped`, failure edges, idempotent stop, and distinct liveness/readiness/health semantics. Depends on 1.1-1.2. **Proof:** table/property tests for every legal/illegal transition and repeated stop request.
- [ ] 1.4 `[chronicle-application]` Define active recorder metadata v1 with recorder/attempt/config/scope/boot-clock/epoch/segment/marker/checkpoint/lag/quota/counters/recovery/failure fields; private atomic temp-write/file-sync/rename/directory-sync; reject leading/contradictory/non-v1 state. Depends on 1.3. **Proof:** round-trip, permission, lag/lead, interrupted-write, checksum/version, and payload-safe snapshots.

## 2. WAL Rotation, Manifest, and Live Snapshots

**Entry:** current WAL framing/group-commit/recovery tests pass unchanged. **Exit:** age rotation, manifest, rebuild, and read-only marker snapshots pass without retention or recorder orchestration.

- [ ] 2.1 `[chronicle-wal]` Add monotonic five-minute-default configurable segment-age trigger on append and periodic tick; rotate only non-empty segment via existing commit/sync/header publication. **Proof:** fake-clock low/high-traffic, empty-segment, size-vs-age tie, sync-count, and crash tests; exact WAL bytes/marker semantics unchanged.
- [ ] 2.2 `[chronicle-wal]` Define WAL manifest v1 DTO: revision/predecessor, recording/epoch/policy, active marker summary, ordered segment identity/range/length/SHA-256/lifecycle/checkpoint/retention/delete fields; discriminator-first non-v1 and contradiction validation. **Proof:** round-trip, ordering, duplicate/overlap, leading-marker, impossible-transition, and payload-safe tests.
- [ ] 2.3 `[chronicle-wal]` Implement private atomic manifest replacement and sealed-segment digest/range capture after successful marker sync. Manifest must never acknowledge or promote data. Depends on 2.1-2.2. **Proof:** write/sync/rename/directory-sync fault matrix and marker-before-manifest ordering assertions.
- [ ] 2.4 `[chronicle-wal]` Implement deterministic manifest rebuild/reconciliation from validated segment bytes and marker chain; lag/missing repairs, leading/contradictory fail closed. Depends on 2.2-2.3. **Proof:** lagging/missing/corrupt manifest fixtures, unchanged-WAL idempotent rerun, and no WAL mutation.
- [ ] 2.5 `[chronicle-wal]` Add live read-only committed snapshot descriptor/API capped at validated marker; concurrent growth/partial suffix is retryable uncertainty, never repair. Startup exclusive recovery remains only repair owner. Depends on 2.3-2.4. **Proof:** writer/reader race tests, marker snapshot stability, no truncate/mutation, and corruption-before-marker failure.
- [ ] 2.6 `[chronicle-wal tests]` Consolidate P1 regression plus P2 age/manifest/live-snapshot tests and verify 64/48/76-byte formats, one-sync policy, sequence, acknowledgement distinction, and final-tail-only repair unchanged. Depends on 2.1-2.5. **Proof:** `cargo test -p chronicle-wal` with deterministic durable-media fixtures.

## 3. RecordingStore Filesystem Foundation

**Entry:** current FilesystemSessionStore tests pass. **Exit:** immutable non-session recorder artifacts use conformance-tested filesystem backend; final sessions and active WAL retain existing owners.

- [ ] 3.1 `[chronicle-storage]` Define minimal RecordingStore artifact identity, kind, deterministic key, size/SHA-256/schema/provenance/retention metadata, and typed outcomes/errors; limit kinds to sealed WAL copies, canonical delta batches, recovery reports, and provenance indexes; exclude active append/lock/repair, mutable checkpoints, final Canonical Session transactions, and generic overwrite. **Proof:** API review plus exhaustive serde/error tests; no new dependency.
- [ ] 3.2 `[chronicle-storage]` Define put-if-absent, verified head/get, bounded diagnostic list, and digest-preconditioned delete semantics; correctness uses explicit manifests, not list/timestamps. Depends on 3.1. **Proof:** in-memory tests for created/matching/conflict/not-found/transient/permanent/uncertain.
- [ ] 3.3 `[chronicle-storage]` Implement filesystem RecordingStore using destination-local private staging, file/directory sync, atomic no-replace, path/symlink confinement, checksum validation, and stale-staging recovery. Depends on 3.2. **Proof:** create/retry/conflict/cross-filesystem/traversal/symlink/permission/write/sync/rename tests.
- [ ] 3.4 `[chronicle-storage]` Implement artifact lifecycle and digest-preconditioned delete/tombstone support without coupling source-WAL, delta, or final-session retention classes. Depends on 3.3. **Proof:** lifecycle table, digest mismatch quarantine, failed-delete retry, and independent-retention tests.
- [ ] 3.5 `[chronicle-storage tests]` Build filesystem conformance suite for put/retry/conflict/integrity/list/delete/key safety/crash/durability uncertainty/lifecycle. Depends on 3.2-3.4. **Proof:** `cargo test -p chronicle-storage` and injected filesystem faults.
- [ ] 3.6 `[chronicle-application, chronicle-storage]` Add state-root/store preflight for private modes, local active-spool semantics, same-filesystem atomic operations, filesystem-domain identity, and unsupported S3 request; keep existing FilesystemSessionStore behavior unchanged. Depends on 3.3-3.5. **Proof:** application tests and unchanged P1 session/inspect/replay fixtures.

## 4. Incremental ETL Checkpoint Model

**Entry:** live marker snapshots and RecordingStore put-if-absent are available. **Exit:** checkpoint v1 round-trips bounded in-flight TCP/HTTP state and rejects ambiguous lineage.

- [ ] 4.1 `[chronicle-etl, chronicle-session]` Inventory exact mutable reconstruction/protocol state needed between committed snapshots; prove every field has deterministic encoding and existing bound. Amend design/spec before implementation if state cannot be bounded. **Proof:** checked mapping plus vector spanning TCP/HTTP fragments and loss windows.
- [ ] 4.2 `[chronicle-etl]` Define incremental checkpoint v1 DTO with predecessor digest, epoch/config/pipeline/canonical versions, exact marker/WAL/segment digests, active connection/directional/lifecycle/loss/protocol correlation state, ID inputs, emitted outputs, source status, counters, and checksum. Depends on 4.1. **Proof:** round-trip, deterministic bytes, discriminator-first non-v1, checksum, duplicate-key, and payload-safe-error tests.
- [ ] 4.3 `[chronicle-session, chronicle-protocol, chronicle-protocol-builtins]` Add explicit export/import of bounded reconstruction and HTTP decoder state without filesystem/application dependency; preserve connection generation, chronology, request queue, body framing, provenance, and completeness. Depends on 4.1-4.2. **Proof:** pause/resume after every event boundary equals uninterrupted decode.
- [ ] 4.4 `[chronicle-etl]` Validate checkpoint lineage against recovery-authoritative snapshot: predecessor, recording/epoch, marker location/digest, segment digests, pipeline versions, output set, and checkpoint not ahead. Depends on 2.5, 4.2. **Proof:** fork, ahead, same-boundary-different-digest, missing-output, wrong-epoch, and valid-resume tests.
- [ ] 4.5 `[chronicle-etl]` Implement private atomic checkpoint write/read after output verification only; include file/directory sync and fault injection. Depends on 3.3, 4.4. **Proof:** pre/post rename/sync failures leave old or exact new checkpoint, never partial success.

## 5. Incremental ETL Processing and Publication

**Entry:** checkpoint model and RecordingStore filesystem backend are complete. **Exit:** recorder-owned live ETL crash-resumes, publishes once, and finalized session matches one-shot output byte-for-byte.

- [ ] 5.1 `[chronicle-etl]` Add recorder-owned incremental processor from prior checkpoint to validated marker snapshot using existing 4096-record batches; exclude markers from protocol input; restore/export state; preserve bounds/loss/provenance. Existing standalone ETL keeps rejecting live writers. Depends on 2.5, 4.3-4.4. **Proof:** live-worker/standalone ownership tests and multiple snapshot-size/poll-cadence equivalence.
- [ ] 5.2 `[chronicle-etl, chronicle-canonical]` Define internal CanonicalDeltaBatch v1 wrapper over existing canonical operation/connection/provenance values with deterministic key/order/digest; no replay or protocol semantic fork. Depends on 5.1. **Proof:** fixed vectors, non-v1 rejection, deterministic ordering, and safe summary.
- [ ] 5.3 `[chronicle-etl, chronicle-storage]` Implement transaction: validate input -> derive outputs/state -> put-if-absent/verify immutable batches -> atomically checkpoint. Depends on 3.3, 4.5, 5.2. **Proof:** fault matrix before/during/after every output/checkpoint operation; retry converges.
- [ ] 5.4 `[chronicle-etl]` Prevent duplicates by exact WAL/checkpoint/output lineage; retain legitimate identical HTTP operations at different provenance and existing retransmission-only dedupe. Depends on 5.3. **Proof:** identical-request-distinct-range, retransmission, retry, checkpoint-fork, and conflicting-output tests.
- [ ] 5.5 `[chronicle-etl, chronicle-storage]` Finalize stopped epoch into existing Canonical Session v1 through unchanged FilesystemSessionStore; compare incremental and clean one-shot session/manifest/IDs/order/digests/completeness/provenance byte-for-byte. Depends on 5.3-5.4. **Proof:** golden equivalence across every commit-boundary partition and crash schedule.
- [ ] 5.6 `[chronicle-application, chronicle-etl]` Add bounded incremental worker lifecycle, retry/backoff, safe-point shutdown, lag thresholds, and health counters. Capture remains independent while reserved WAL headroom permits; lag never authorizes cleanup. Depends on 5.3. **Proof:** worker crash/restart, store outage, shutdown safe point, growing lag, and bounded memory/task tests.
- [ ] 5.7 `[chronicle-application]` Reconcile delta publication/checkpoint on startup: adopt exact orphan output, repair missing checkpoint, reject conflict/fork/ahead. Depends on 5.3-5.6. **Proof:** startup at every crash boundary and idempotent second recovery.
- [ ] 5.8 `[chronicle-etl tests]` Add fragmented long-lived HTTP across segments, epoch-boundary incompleteness, loss windows, idle/byte/chunk/body/operation bounds, polling permutations, standalone-live rejection, and one-shot equivalence. Depends on 5.1-5.7. **Proof:** `cargo test -p chronicle-session -p chronicle-etl -p chronicle-protocol-builtins`.

## 6. Retention, Quota, and Recorder Orchestration

**Entry:** WAL manifest/snapshots, final-session publication, and incremental checkpoint/output transaction pass independently. **Exit:** continuous recorder rolls epochs, safely cleans finalized data, survives restart, and shuts down in ordered state.

- [ ] 6.1 `[chronicle-wal, chronicle-application]` Implement segment states `active/sealed/processed/retention_eligible/deleting/deleted/quarantined` with proof-gated monotonic transitions and bounded tombstones. `processed` requires exact delta/checkpoint proof; finalized-epoch retention eligibility additionally requires verified Canonical Session manifest/payload integrity. Depends on 2.4, 5.3, 5.5. **Proof:** transition table, delta-only retention denial, retention-age-before-checkpoint denial, quarantine preservation, and tombstone bounds.
- [ ] 6.2 `[chronicle-wal, chronicle-application]` Implement crash-safe finalized-epoch cleanup transaction: durable deleting intent -> same-filesystem trash rename + dir sync -> deleted tombstone -> unlink + dir sync; active-prefix deletion remains out of scope. Depends on 3.4, 6.1. **Proof:** crash/failure at every step, missing-without-intent corruption, digest-precondition mismatch, and deterministic completion/rollback.
- [ ] 6.3 `[chronicle-application, chronicle-wal, chronicle-storage]` Add synchronized quota/minimum-free reservation authority per filesystem domain covering concurrent WAL, manifest/checkpoint, RecordingStore, co-located final session, staging, and trash peaks while retaining per-epoch 4 GiB cap; separate roots use independent reserves. Eligible cleanup runs first; insufficient headroom stops intake. Depends on 3.6, 5.6, 6.2. **Proof:** exact-bound/property tests, concurrent ETL/WAL reservations, separate-root isolation, temp peaks, retain-mode no-delete, no-eligible stop, and quota restored by cleanup.
- [ ] 6.4 `[chronicle-application, chronicle-capture]` Introduce recorder orchestration around existing `record_production`/bounded ingest without parallel queue or capture schema; keep one source attached across epoch writer handoff and assign each accepted observation exactly once. Depends on 1.3-1.4, 2.5, 5.6, 6.3. **Proof:** deterministic fake source boundary races, queue saturation, no duplicate/missing assignment, and unchanged P1 one-shot tests.
- [ ] 6.5 `[chronicle-application, chronicle-wal]` Implement bounded epoch rollover at configured age/bytes no greater than P1 one-hour/4 GiB; persist ordinal/predecessor; finalize old and durably open new before routing remaining events. On next-epoch open failure, persist exact metadata-only `epoch_rollover_failure` counts/bytes/timestamp interval or ambiguity for accepted unpersisted entries. Depends on 6.4. **Proof:** three epochs, exact boundary counters, rollover failure with queued events, and long-lived connection incomplete-boundary tests.
- [ ] 6.6 `[chronicle-application, chronicle-capture-ebpf]` Compose startup: lease -> state/WAL/manifest/checkpoint/cleanup recovery -> synchronized quota reservations -> durably appendable epoch -> capture preflight/attach -> worker resume -> running/readiness. Depends on 5.7, 6.3-6.5. **Proof:** fault at every startup step, no attach before WAL, no early readiness, partial-link cleanup, deterministic restart.
- [ ] 6.7 `[chronicle-application, chronicle-cli]` Preserve first-signal graceful and second-signal forced behavior; order stop intake -> final sample -> queue drain -> marker sync -> epoch seal -> worker safe point -> detach -> atomically persist `stopped` -> release lease; enforce timeout. Depends on 5.6, 6.6. **Proof:** subprocess SIGINT/SIGTERM/second-signal/timeout/final-sample/WAL/checkpoint/metadata failure matrix and no lease-before-stopped race.
- [ ] 6.8 `[chronicle-wal, chronicle-application tests]` Consolidate manifest lifecycle/retention/quota/recovery fault matrix with P1 WAL regression; verify no unsafe deletion, unaccounted rollover loss, format change, or acknowledgement inference. Depends on 6.1-6.7. **Proof:** deterministic durable-media and filesystem crash fixtures.

## 7. CLI, Diagnostics, Deployment, and Operations

**Entry:** continuous recorder application API passes rootless orchestration tests. **Exit:** foreground service/status commands, doctor probes, systemd model, and runbook are reviewable and rootless-tested.

- [ ] 7.1 `[chronicle-application]` Add safe bounded recorder status with liveness/readiness/health, IDs, config digest, commit/checkpoint lag, segment counts, per-filesystem quota/free bytes, counters, recovery/cleanup/publication, stale-owner detection, and remediation. Depends on 6.7. **Proof:** healthy/degraded/failed/stale deterministic human+JSON model tests with no sensitive values.
- [ ] 7.2 `[chronicle-cli]` Add foreground `recorder --config FILE`; delegate to application; map startup/runtime/signal exits; emit bounded startup result; avoid self-daemon/PID/control server. Depends on 7.1. **Proof:** Clap/subprocess ready/failure/SIGINT/SIGTERM/second-signal/atomic-JSON tests.
- [ ] 7.3 `[chronicle-cli, chronicle-application]` Add non-mutating `recorder-status --state-root DIR` via shared atomic renderer; no lease mutation/repair/cleanup/ETL/capture. Depends on 7.1. **Proof:** live/stale/missing/corrupt/human/JSON/stdout-failure tests.
- [ ] 7.4 `[chronicle-application, chronicle-wal, chronicle-storage]` Extend doctor with config/lease/state/manifest/checkpoint/retention/per-filesystem quota/min-free/filesystem-store/same-filesystem/trash/placement probes using existing status model and no side effects. Depends on 3.5-3.6, 6.8. **Proof:** supported/warning/unsupported/not-checked precedence, cleanup, active-owner, unsafe-policy, and recorder-in-subtree tests.
- [ ] 7.5 `[chronicle-cli tests]` Prove P1 `record`, standalone-live-rejecting `etl`, `doctor`, `inspect`, and `replay` contracts remain unchanged and finalized incremental result works with inspect/replay. Depends on 5.5, 7.2-7.4. **Proof:** existing CLI contract suite plus P2 regression cases.
- [ ] 7.6 `[packaging/docs]` Add reviewed systemd `Type=simple` service/template example: foreground command, dedicated identity, `StateDirectory`, config path, least capabilities, recorder outside captured subtree, restart backoff/start limit, SIGTERM/timeout, resource controls, private modes, and explicit `recorder-status` readiness polling. No `sd_notify` or Kubernetes manifest. Depends on 7.2-7.4. **Proof:** static unit validation and documented install/start/readiness/stop/status commands.
- [ ] 7.7 `[docs]` Update architecture/WAL/operations/capability/security docs with lifecycle/data/state diagrams, manifest/checkpoint/store formats, node-vs-sidecar decision, configuration, resource expectations, readiness/health, per-filesystem quota/retention, failure/recovery, sensitive-data warnings, unsupported models, and upgrade/rollback. Depends on 7.6. **Proof:** links/examples/schema references validated; no production-scale overclaim.
- [ ] 7.8 `[docs/scripts]` Add operator runbook for preflight/start/readiness polling/status/stop/restart, ETL lag, disk pressure, corruption quarantine, forced termination, retention review, upgrade/rollback, evidence export, and leak checks; identify platform/application/Chronicle owners. Depends on 7.7. **Proof:** rootless/static command validation and privileged walkthrough task 8.4.

## 8. Validation and Acceptance

**Entry:** phases 1-7 complete. **Exit:** deterministic rootless evidence, exact-commit privileged evidence, independent review, and strict OpenSpec validation complete.

- [ ] 8.1 `[workspace tests]` Add rootless fake capture -> three epochs -> size/age rotation -> incremental ETL -> final canonical session -> inspect/replay; prove one-shot equivalence, no duplicates, bounded resources, safe rollover-loss accounting, and payload-safe outputs. Depends on 1-7. **Proof:** `cargo test --workspace --all-features --locked`.
- [ ] 8.2 `[fault/property tests]` Run durable crash matrix around frame/marker sync, manifest, epoch handoff, delta publication, checkpoint, final session, processed transition, cleanup, metadata, and signal drain; assert convergence/no silent loss/unsafe deletion/lease race. Depends on 8.1. **Proof:** checked-in deterministic fixtures/property seeds and exact recovered lineage assertions.
- [ ] 8.3 `[storage/ETL conformance]` Run filesystem RecordingStore conformance, checkpoint fork/corruption, synchronized and separate quota domains, disk-full at every persistence boundary, and P1 regression suites. Depends on 8.1-8.2. **Proof:** retained CI logs summarized by scenarios, not privileged claims.
- [ ] 8.4 `[scripts/acceptance/p2-privileged.sh, scripts/tests, evidence]` Create clean-exact-commit Ubuntu 24.04 privileged acceptance under systemd `Type=simple`: real cgroup workload/eBPF; readiness polling; at least three epochs and size+age rotations; live incremental worker; standalone ETL live rejection; SIGTERM; process kill/restart at WAL/publication/checkpoint/final-session/cleanup points; one full host/VM reboot with persistent state followed by systemd restart/recovery before readiness; quota/min-free pressure; corruption quarantine; inspect/replay; process/cgroup/eBPF/temp cleanup. Depends on 8.1-8.3. **Proof:** retained machine-readable pre-reboot/post-reboot report/artifacts/checksums under `evidence/privileged/<commit>/...` with kernel/arch/capabilities/unit/config digest and checked/not-checked claims.
- [ ] 8.5 `[scripts/acceptance/p2-multipass.sh]` Add reproducible clean-tree VM runner that clones exact SHA, executes 8.4 across required VM reboot/resume, hashes/transfers evidence, and refuses dirty or mismatched commit. Depends on 8.4. **Proof:** retained successful supported-environment run including host-reboot recovery.
- [ ] 8.6 `[CI/tooling]` Run formatting, locked workspace check, Clippy all targets/features with warnings denied, all-feature tests, isolated eBPF build, acceptance-runner tests, and targeted LSP/static diagnostics. Depends on 8.1-8.5. **Proof:** all commands pass; fixture-only lanes label privileged runtime not checked.
- [ ] 8.7 `[OpenSpec]` Run `openspec validate add-continuous-recorder-foundation --strict`; confirm proposal/design/spec/task consistency and checkbox evidence. This is SDD validation only and does not satisfy 8.4-8.5. Depends on artifact/task updates. **Proof:** strict validator passes.
- [ ] 8.8 `[independent review]` Review durability authority, checkpoint/publication/final-session ordering, cleanup/quota safety, deterministic equivalence, storage ownership, privilege/sensitive-data posture, and acceptance evidence. Resolve every blocker before archive. Depends on 8.4-8.7. **Proof:** retained review findings/dispositions and no unresolved blocker.
