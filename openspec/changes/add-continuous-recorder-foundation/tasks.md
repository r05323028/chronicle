# Implementation Tasks

Tasks are grouped by production implementation phase. Numeric task IDs remain stable for traceability and do not imply section order. Each checkbox names owning area and required proof. OpenSpec validation proves artifacts only; privileged task completion requires retained supported-Linux evidence explicitly named by that task.

## 1. Recorder State Primitives

**Entry:** archived P1 production pipeline and current rootless suites pass. **Exit:** configuration, lease, lifecycle state, and metadata primitives are deterministic and fault-tested without starting capture.

- [x] 1.1 `[chronicle-application]` Define recorder configuration v1 for one cgroup scope/state root, canonical filesystem-domain identity/domain-lock root, epoch/segment/quota/retention/ETL/store/shutdown bounds, and one quota/minimum-free reserve per filesystem domain; normalize stable digest; reject unknown fields/version, contradictory bounds, unsafe paths, missing explicit retention mode, and scope/state/domain identity mismatch. **Proof:** round-trip, non-v1, invalid-bound, filesystem-domain/lock-root, digest-stability, and safe-error tests.
- [x] 1.2 `[chronicle-application]` Add OS-released exclusive filesystem-domain lease at deterministic `<domain-lock-root>/.chronicle-domain.lock`, with subordinate recorder-state lease separate from per-recording WAL lock; resolve domain identity consistently and reject concurrent recorder/standalone mutator before mutation/attach; never use PID existence as authority. Depends on 1.1. **Proof:** subprocess same-domain/different-domain ownership, lock-order, concurrent-owner, crash-release, PID-reuse, standalone-mutator rejection, and no-mutation tests.
- [x] 1.3 `[chronicle-application]` Define lifecycle state machine `starting -> recovering -> running -> draining -> stopped`, failure edges, idempotent stop, and separate liveness, capture-readiness, processing-readiness, and policy-derived health semantics. Depends on 1.1-1.2. **Proof:** table/property tests for every legal/illegal transition, repeated stop request, ETL lag without capture failure, and readiness-field precedence.
- [x] 1.4 `[chronicle-application]` Define active recorder metadata v1 with recorder/attempt/config/scope/boot-clock/epoch/segment/marker/`IncrementalEtlCheckpoint v1`/capture-readiness/processing-readiness/health/lag/quota/counters/recovery/failure fields; private atomic temp-write/file-sync/rename/directory-sync; reject leading/contradictory/non-v1 state. Depends on 1.3. **Proof:** round-trip, permission, lag/lead, interrupted-write, checksum/version, readiness split, and payload-safe snapshots.

## 2. WAL Rotation, Manifest, and Live Snapshots

**Entry:** current WAL framing/group-commit/recovery tests pass unchanged. **Exit:** age rotation, manifest, rebuild, and read-only marker snapshots pass without retention or recorder orchestration.

- [x] 2.1 `[chronicle-wal]` Add monotonic five-minute-default configurable segment-age trigger on append and periodic tick; rotate only non-empty segment via existing commit/sync/header publication. **Proof:** fake-clock low/high-traffic, empty-segment, size-vs-age tie, sync-count, and crash tests; exact WAL bytes/marker semantics unchanged.
- [x] 2.2 `[chronicle-wal]` Define WAL manifest v1 DTO: revision/predecessor, recording/epoch/policy, active marker summary, ordered segment identity/range/length/SHA-256/lifecycle/checkpoint/retention/delete fields; discriminator-first non-v1 and contradiction validation. **Proof:** round-trip, ordering, duplicate/overlap, leading-marker, impossible-transition, and payload-safe tests.
- [x] 2.3 `[chronicle-wal]` Implement private atomic manifest replacement and sealed-segment digest/range capture after successful marker sync. Manifest must never acknowledge or promote data. Depends on 2.1-2.2. **Proof:** write/sync/rename/directory-sync fault matrix and marker-before-manifest ordering assertions.
- [x] 2.4 `[chronicle-wal]` Implement deterministic manifest rebuild/reconciliation from validated segment bytes and marker chain; lag/missing repairs, leading/contradictory fail closed. Depends on 2.2-2.3. **Proof:** lagging/missing/corrupt manifest fixtures, unchanged-WAL idempotent rerun, and no WAL mutation.
- [x] 2.5 `[chronicle-wal]` Add live read-only committed snapshot descriptor/API capped at validated marker; concurrent growth/partial suffix is retryable uncertainty, never repair. Startup exclusive recovery remains only repair owner. Depends on 2.3-2.4. **Proof:** writer/reader race tests, marker snapshot stability, no truncate/mutation, and corruption-before-marker failure.
- [x] 2.6 `[chronicle-wal tests]` Consolidate P1 regression plus P2 age/manifest/live-snapshot tests and verify 64/48/76-byte formats, one-sync policy, sequence, acknowledgement distinction, and final-tail-only repair unchanged. Depends on 2.1-2.5. **Proof:** `cargo test -p chronicle-wal` with deterministic durable-media fixtures.

## 3. Recorder Orchestration

**Entry:** Phases 1-2 complete. **Exit:** Continuous Recorder Foundation — the recorder can run continuously with filesystem ownership, lifecycle recovery, WAL epoch rollover, deterministic shutdown, and bounded status. No incremental ETL or retention deletion is required.

- [x] 6.4 `[chronicle-application, chronicle-capture]` Introduce recorder orchestration around existing `record_production`/bounded ingest without parallel queue or capture schema; keep one source attached across epoch writer handoff and assign each accepted observation exactly once. Provide ETL safe-point hook without requiring incremental ETL correctness. Depends on 1.3-1.4, 2.5. **Proof:** deterministic fake source boundary races, queue saturation, no duplicate/missing assignment, durable outcome-range handoff, and unchanged P1 one-shot tests.
- [x] 6.5 `[chronicle-application, chronicle-wal]` Implement bounded epoch rollover at configured age/bytes no greater than P1 one-hour/4 GiB; persist ordinal/predecessor; define accepted observation at recorder-owned queue admission with stable ingest ordinal, payload length, capture timestamp, and source metadata; persist durable outcome ranges mapping admitted observations to old/new authoritative-marker assignment or loss before reporting rollover success; finalize old and durably open new before routing remaining events. On next-epoch open or outcome-journal failure, preserve prior authority and count admitted-but-unproven observations as exact metadata-only `epoch_rollover_failure` counts/bytes/timestamp interval or ambiguity. Depends on 6.4. **Proof:** three epochs, queue admission before rollover, queue admission after rollover, exact loss counters/bytes/timestamp bounds, no duplicate assignment between epochs, outcome-persistence crash/failure, rollover failure with queued observations, and long-lived connection incomplete-boundary tests.
- [x] 6.6a `[chronicle-application, chronicle-capture-ebpf]` Compose pre-ETL recorder startup: domain lease -> state/WAL/manifest recovery -> durably appendable epoch -> capture preflight/attach -> running; publish capture readiness separately from processing readiness. This foundation path MUST NOT require incremental ETL correctness, production quota enforcement, or retention cleanup. Depends on 1.2-1.4, 2.4-2.5, 6.4-6.5. **Proof:** fault at every foundation startup step, no attach before durable WAL appendability, deterministic restart recovery, and capture-readiness gating.
- [x] 6.7 `[chronicle-application, chronicle-cli]` Preserve first-signal graceful and second-signal forced behavior; order stop intake -> final sample -> queue drain -> marker sync -> epoch seal -> ETL safe-point hook -> detach -> atomically persist `stopped` -> release domain/state leases; enforce timeout. Depends on 1.3-1.4, 6.5. **Proof:** subprocess SIGINT/SIGTERM/second-signal/timeout/final-sample/WAL/outcome-journal/metadata failure matrix and no domain-lease-before-stopped race.
- [x] 7.1 `[chronicle-application]` Add safe bounded recorder status model with liveness, capture readiness, processing readiness/unknown, overall health, IDs, config digest, commit/`IncrementalEtlCheckpoint v1` lag when available, segment counts, per-filesystem quota/free bytes, counters, recovery/cleanup/publication, stale-owner detection, and remediation. Processing, quota, and cleanup fields may remain unknown/not-active until later phases. Depends on 1.4, 6.6a, 6.7. **Proof:** healthy/recovering/degraded/failed/stale deterministic human+JSON model tests, ETL-lag-with-capture-ready case, and no sensitive values.

## Implementation Gate A: Continuous Recorder Lifecycle

- [x] **Gate A: Continuous recorder lifecycle** — establish recorder lease, lifecycle state machine, WAL manifest, epoch rollover, pre-ETL startup/restart recovery, shutdown handling, and status model. Depends on 1.1-1.4, 2.1-2.6, 6.4-6.6a, 6.7, 7.1. **Proof:** lifecycle state tests pass; restart recovery evidence exists; no capture occurs before durable WAL readiness. This gate MUST NOT depend on retention, quota enforcement, cleanup, or incremental ETL correctness. It uses only an ETL safe-point hook. Dependent implementation MUST NOT continue until evidence exists; if it fails, update design/specification before proceeding. Evidence: `evidence/gate-a/rootless-2026-08-01.json`.

## 4. RecordingStore Filesystem Foundation

**Entry:** Gate A lifecycle foundation is approved and current FilesystemSessionStore tests pass. **Exit:** immutable non-session recorder artifacts use conformance-tested filesystem backend; final sessions and active WAL retain existing owners.

- [x] 3.1 `[chronicle-storage]` Define RecordingStore ownership boundary: own immutable artifact storage semantics, artifact identity, integrity verification, artifact lifecycle metadata, and filesystem backend implementation; exclude active WAL append/commit-marker authority/repair, incremental checkpoint authority, final Canonical Session publication, and generic overwrite. Allow optional future-compatible sealed WAL copies plus canonical delta batches, recovery reports, and provenance indexes; sealed WAL copies MUST NOT be required for retention correctness. **Proof:** API review plus exhaustive serde/error tests; no new dependency.
- [x] 3.2 `[chronicle-storage]` Define put-if-absent, verified head/get, bounded diagnostic list, and digest-preconditioned delete semantics; correctness uses explicit manifests, not list/timestamps. Depends on 3.1. **Proof:** in-memory tests for created/matching/conflict/not-found/transient/permanent/uncertain.
- [x] 3.3 `[chronicle-storage]` Implement filesystem RecordingStore using destination-local private staging, file/directory sync, atomic no-replace, path/symlink confinement, checksum validation, and stale-staging recovery. Depends on 3.2. **Proof:** create/retry/conflict/cross-filesystem/traversal/symlink/permission/write/sync/rename tests.
- [x] 3.4 `[chronicle-storage]` Implement artifact lifecycle and digest-preconditioned delete/tombstone support without coupling source-WAL, delta, or final-session retention classes. Depends on 3.3. **Proof:** lifecycle table, digest mismatch quarantine, failed-delete retry, and independent-retention tests.
- [x] 3.5 `[chronicle-storage tests]` Build filesystem conformance suite for put/retry/conflict/integrity/list/delete/key safety/crash/durability uncertainty/lifecycle and ownership boundaries. Depends on 3.2-3.4. **Proof:** `cargo test -p chronicle-storage` and injected filesystem faults; tests prove RecordingStore cannot bypass WAL commit/repair authority, checkpoint ordering remains incremental-ETL-owned, and final Canonical Session publication remains `FilesystemSessionStore`-owned.
- [x] 3.6 `[chronicle-application, chronicle-storage]` Add state-root/store preflight for private modes, local active-spool semantics, same-filesystem atomic operations, filesystem-domain identity, one mutating owner per domain, standalone-mutator rejection, optional sealed-WAL-copy behavior, unsupported S3 request, and rejection of attempts to use RecordingStore as WAL/checkpoint/final-session authority; keep existing FilesystemSessionStore behavior unchanged. Depends on 3.3-3.5. **Proof:** application tests for owner/domain isolation and optional copies, authority-boundary rejection, plus unchanged P1 session/inspect/replay fixtures.

## 5. Incremental ETL Checkpoint Model

**Entry:** Gate A and RecordingStore filesystem foundation are complete. **Exit:** `IncrementalEtlCheckpoint v1` round-trips bounded in-flight TCP/HTTP state and rejects ambiguous lineage without extending `FinalSessionCheckpoint v1`.

- [x] 4.1 `[chronicle-etl, chronicle-session]` Inventory exact mutable reconstruction/protocol state needed between committed snapshots; prove every field has deterministic encoding and existing bound. Amend design/spec before implementation if state cannot be bounded. Depends on Gate A, 3.1-3.6. **Proof:** checked mapping plus vector spanning TCP/HTTP fragments and loss windows.
- [x] 4.2 `[chronicle-etl]` Define distinct `IncrementalEtlCheckpoint v1` DTO with its own discriminator/owner/lifecycle: WAL marker lineage, segment digest lineage, decoder reconstruction, TCP direction, HTTP correlation, loss-window state, predecessor digest, epoch/config/pipeline/canonical versions, exact marker/WAL/segment digests, active connection/directional/lifecycle/protocol state, ID inputs/counters, emitted delta-batch references, source status, and checksum. Do not extend or dual-interpret `FinalSessionCheckpoint v1`. Depends on 4.1. **Proof:** round-trip, deterministic bytes, discriminator-first non-v1, checksum, duplicate-key, cross-artifact rejection, and payload-safe-error tests.
- [x] 4.3 `[chronicle-session, chronicle-protocol, chronicle-protocol-builtins]` Add explicit export/import of bounded reconstruction and HTTP decoder state without filesystem/application dependency; preserve connection generation, chronology, request queue, body framing, provenance, and completeness. Depends on 4.1-4.2. **Proof:** pause/resume after every event boundary equals uninterrupted decode.
- [x] 4.4 `[chronicle-etl]` Validate checkpoint lineage against recovery-authoritative snapshot: predecessor, recording/epoch, marker location/digest, segment digests, pipeline versions, output set, and checkpoint not ahead. Depends on 2.5, 4.2. **Proof:** fork, ahead, same-boundary-different-digest, missing-output, wrong-epoch, and valid-resume tests.
- [x] 4.5 `[chronicle-etl]` Implement private atomic `IncrementalEtlCheckpoint v1` write/read after output verification only; include file/directory sync and fault injection, while leaving `FinalSessionCheckpoint v1` lifecycle unchanged. Depends on Gate B, 3.3, 4.4. **Proof:** pre/post rename/sync failures leave old or exact new checkpoint, never partial success or dual interpretation.

## Implementation Gate B: Incremental ETL Feasibility

- [x] **Gate B: Incremental ETL feasibility** — before full incremental ETL, verify every mutable decoder state has bounded representation, deterministic serialization, and pause/resume equivalence. Depends on Gate A, 4.1-4.4. **Proof:** state inventory document, serialization vectors, and fragmented HTTP traffic equivalence tests. If bounded state cannot be proven, stop implementation and update design first. No retention or quota dependency. Evidence: `cargo test -p chronicle-etl` (13 passed).

## 6. Incremental ETL Processing and Publication

**Entry:** Gate B, checkpoint model, and RecordingStore filesystem backend are complete. **Exit:** Incremental Processing Foundation — committed WAL snapshots are processed incrementally with bounded checkpoint state, deterministic publication, crash recovery, and one-shot equivalence.

- [x] 5.1 `[chronicle-etl]` Add recorder-owned incremental processor from prior checkpoint to validated marker snapshot using existing 4096-record batches; exclude markers from protocol input; restore/export state; preserve bounds/loss/provenance. Existing standalone ETL keeps rejecting live writers. Depends on Gate B, 2.5, 4.3-4.4. **Proof:** live-worker/standalone ownership tests and multiple snapshot-size/poll-cadence equivalence.
- [x] 5.2 `[chronicle-etl, chronicle-canonical]` Define internal CanonicalDeltaBatch v1 wrapper over existing canonical operation/connection/provenance values with deterministic key/order/digest; no replay or protocol semantic fork. Depends on 5.1. **Proof:** fixed vectors, non-v1 rejection, deterministic ordering, and safe summary.
- [x] 5.3 `[chronicle-etl, chronicle-storage]` Implement transaction: validate input -> derive outputs/state -> put-if-absent/verify immutable batches -> atomically write `IncrementalEtlCheckpoint v1`. Depends on 3.3, 4.5, 5.2. **Proof:** fault matrix before/during/after every output/checkpoint operation; retry converges without changing `FinalSessionCheckpoint v1`.
- [x] 5.4 `[chronicle-etl]` Prevent duplicates by exact WAL/checkpoint/output lineage; retain legitimate identical HTTP operations at different provenance and existing retransmission-only dedupe. Depends on 5.3. **Proof:** identical-request-distinct-range, retransmission, retry, checkpoint-fork, and conflicting-output tests.
- [x] 5.5 `[chronicle-etl, chronicle-storage]` Finalize stopped epoch into existing Canonical Session v1 through unchanged FilesystemSessionStore; compare incremental and clean one-shot session/manifest/IDs/order/digests/completeness/provenance byte-for-byte. Depends on 5.3-5.4. **Proof:** golden equivalence across every commit-boundary partition and crash schedule.
- [x] 5.6 `[chronicle-application, chronicle-etl]` Add bounded incremental worker lifecycle, retry/backoff, safe-point shutdown, lag thresholds, and health counters. Capture remains independent while reserved WAL headroom permits; lag never authorizes cleanup. Depends on 5.3. **Proof:** worker crash/restart, store outage, shutdown safe point, growing lag, and bounded memory/task tests.
- [x] 5.7 `[chronicle-application]` Reconcile delta publication/checkpoint on startup: adopt exact orphan output, repair missing checkpoint, reject conflict/fork/ahead. Depends on 5.3-5.6. **Proof:** startup at every crash boundary and idempotent second recovery.
- [x] 5.8 `[chronicle-etl tests]` Add fragmented long-lived HTTP across segments, epoch-boundary incompleteness, loss windows, idle/byte/chunk/body/operation bounds, polling permutations, standalone-live rejection, and one-shot equivalence. Depends on 5.1-5.7. **Proof:** `cargo test -p chronicle-session -p chronicle-etl -p chronicle-protocol-builtins`.

## Implementation Gate C: Incremental Publication Correctness

- [x] **Gate C: Incremental publication correctness** — prove deterministic `CanonicalDeltaBatch` output, put-if-absent behavior, checkpoint transaction ordering, crash recovery convergence, and one-shot ETL equivalence. Depends on Gate B, 3.1-3.6, 4.5, 5.1-5.7. **Proof:** retained fault matrix and deterministic equivalence evidence before retention transitions consume incremental output. No quota dependency. Evidence: `cargo test -p chronicle-application -p chronicle-wal -p chronicle-storage -p chronicle-etl -p chronicle-session -p chronicle-protocol-builtins` (214 passed).

## 7. Retention and Quota

**Entry:** Gate C and Incremental Processing Foundation are complete. **Exit:** Retention Safety — data cleanup is enabled only after committed evidence verification, ETL publication proof, checkpoint durability, lifecycle compaction, and quota ownership are proven.

- [x] 6.1 `[chronicle-wal, chronicle-application]` Implement segment states `active/sealed/processed/retention_eligible/deleting/deleted/quarantined` with proof-gated monotonic transitions and bounded tombstones. `processed` requires exact delta/`IncrementalEtlCheckpoint v1` proof; finalized-epoch retention eligibility additionally requires verified Canonical Session manifest/payload integrity. Keep retention deletion activation disabled until Gate D passes. Depends on Gate C, 2.4, 5.3, 5.5. **Proof:** transition table, delta-only retention denial, retention-age-before-checkpoint denial, quarantine preservation, tombstone bounds, and protected-lineage preservation.
- [x] 6.2 `[chronicle-wal, chronicle-application]` Implement crash-safe finalized-epoch cleanup transaction: durable deleting intent -> same-filesystem trash rename + dir sync -> deleted tombstone -> unlink + dir sync; active-prefix deletion remains out of scope. Keep production deletion disabled until Gate D passes. Depends on 3.4, 6.1. **Proof:** crash/failure at every step, missing-without-intent corruption, digest-precondition mismatch, and deterministic completion/rollback.
- [x] 6.2a `[chronicle-wal, chronicle-application]` Implement bounded lifecycle index and tombstone compaction for `epochs.json`, lineage history, retention metadata, and finalized cleanup records after retention semantics exist. Enforce configured maximum byte/entry limits; compact only finalized epochs retained according to policy with cleanup complete and tombstones persisted; preserve first retained epoch, last compacted epoch, predecessor, digest-chain root/tip, cleanup summary, active epochs, and contradiction-detection ranges. Record monotonic generation, transaction ID, source-index revision/digest, candidate digest, and validate old-vs-new conflict selection. Use deterministic atomic crash-recoverable replacement without historical scans for startup/readiness/status. If protected history cannot fit after legal compaction, stop new admission/epoch creation and fail closed with bounded metadata-limit evidence rather than delete protected lineage or exceed limits. Depends on 6.1-6.2. **Proof:** synthetic multi-year epoch history, maximum size/entry enforcement, deterministic repeated compaction, crash recovery during compaction, stale/conflicting candidate rejection, protected-history exhaustion behavior, cleanup/tombstone preservation, and lineage fork/missing-commit/digest-mismatch/incomplete-cleanup contradiction detection.
- [x] 6.3 `[chronicle-application, chronicle-wal, chronicle-storage]` Add one-owner-per-filesystem-domain quota/minimum-free reservation authority covering concurrent WAL, manifest/checkpoint, RecordingStore, co-located final session, staging, and trash peaks while retaining per-epoch 4 GiB cap; recorder lease covers domain; multiple recorders sharing a WAL/store filesystem are unsupported; standalone mutators reject a live-owned domain; separate domains use independent reserves. Implement reservations and proof paths with production quota enforcement disabled until Gate D passes. Eligible cleanup runs first; insufficient headroom stops intake. Depends on 3.6, 5.6, 6.2a. **Proof:** exact-bound/property tests, concurrent same-owner ETL/WAL reservations, live-recorder standalone-mutator rejection, separate-root isolation, temp peaks, retain-mode no-delete, no-eligible stop, and quota restored by cleanup.

## Implementation Gate D: Retention and Quota Activation

- [x] **Gate D: Retention and quota activation** — final safety gate before production deletion, quota enforcement, or retention transitions. Depends on Gate C, 6.1-6.3. **Proof:** cleanup proof chain, one-owner quota model, lifecycle compaction, tombstone correctness, crash matrix, no deletion of unverified data, and deterministic recovery evidence. Retention/quota tasks may implement disabled test/proof paths, but this gate alone enables production deletion, quota enforcement, and retention transitions. Evidence: `evidence/gate-d/5352df6-retention-quota.json`.

- [x] 6.6 `[chronicle-application, chronicle-capture-ebpf]` Complete post-foundation startup integration: domain lease -> state/WAL/manifest/`IncrementalEtlCheckpoint v1`/cleanup recovery -> synchronized quota reservations -> durably appendable epoch -> capture preflight/attach -> ETL worker resume -> running; publish capture readiness and processing readiness separately. Depends on Gate D, 5.7, 6.3-6.5, 6.6a. **Proof:** fault at every startup step, no attach before WAL, capture readiness only after appendability/attachment, processing readiness independently gated, partial-link cleanup, deterministic restart.
- [x] 6.8 `[chronicle-wal, chronicle-application tests]` Consolidate manifest lifecycle/retention/quota/recovery fault matrix with P1 WAL regression; verify no unsafe deletion, unaccounted rollover loss, format change, or acknowledgement inference. Depends on Gate D, 6.1-6.7. **Proof:** deterministic durable-media and filesystem crash fixtures. Evidence: `evidence/gate-d/cd1d18d-privileged.json`.

## 8. Diagnostics, Production Operations, and Deployment

**Entry:** Continuous Recorder Foundation, Incremental Processing Foundation, and Retention Safety milestones are complete. **Exit:** foreground service/status commands, doctor probes, systemd model, and runbook are reviewable and rootless-tested.

- [x] 7.2 `[chronicle-cli]` Add foreground `recorder --config FILE`; delegate to application; map startup/runtime/signal exits; emit bounded startup result; avoid self-daemon/PID/control server. Depends on 7.1, 6.6. **Proof:** Clap/subprocess ready/failure/SIGINT/SIGTERM/second-signal/atomic-JSON tests.
- [x] 7.3 `[chronicle-cli, chronicle-application]` Add non-mutating `recorder-status --state-root DIR` via shared atomic renderer; no lease mutation/repair/cleanup/ETL/capture. Depends on 7.1. **Proof:** live/stale/missing/corrupt/human/JSON/stdout-failure tests.
- [x] 7.4 `[chronicle-application, chronicle-wal, chronicle-storage]` Extend doctor with config/lease/state/manifest/checkpoint/retention/per-filesystem quota/min-free/filesystem-store/same-filesystem/trash/placement probes using existing status model and no side effects. Depends on 3.5-3.6, 6.8. **Proof:** supported/warning/unsupported/not-checked precedence, cleanup, active-owner, unsafe-policy, and recorder-in-subtree tests.
- [x] 7.5 `[chronicle-cli tests]` Prove P1 `record`, standalone-live-rejecting `etl`, `doctor`, `inspect`, and `replay` contracts remain unchanged and finalized incremental result works with inspect/replay. Depends on 5.5, 7.2-7.4. **Proof:** existing CLI contract suite plus P2 regression cases.
- [x] 7.6 `[packaging/docs]` Add reviewed systemd `Type=simple` service/template example: foreground command, dedicated identity, `StateDirectory`, config path, least capabilities, recorder outside captured subtree, restart backoff/start limit, SIGTERM/timeout, resource controls, private modes, and explicit `recorder-status` readiness polling. No `sd_notify` or Kubernetes manifest. Depends on 7.2-7.4. **Proof:** static unit validation and documented install/start/readiness/stop/status commands.
- [x] 7.7 `[docs]` Update architecture/WAL/operations/capability/security docs with lifecycle/data/state diagrams, manifest/checkpoint/store formats, node-vs-sidecar decision, configuration, resource expectations, readiness/health, per-filesystem quota/retention, failure/recovery, sensitive-data warnings, unsupported models, and upgrade/rollback. Depends on 7.6. **Proof:** links/examples/schema references validated; no production-scale overclaim.
- [x] 7.8 `[docs/scripts]` Add operator runbook for preflight/start/readiness polling/status/stop/restart, ETL lag, disk pressure, corruption quarantine, forced termination, retention review, upgrade/rollback, evidence export, and leak checks; identify platform/application/Chronicle owners. Depends on 7.7. **Proof:** rootless/static command validation and privileged walkthrough task 8.4.

## 9. Validation and Acceptance

**Entry:** phases 1-8 complete, with Gates A-D satisfied in order. **Exit:** deterministic rootless evidence, exact-commit privileged evidence, independent review, and strict OpenSpec validation complete.

- [x] 8.1 `[workspace tests]` Add rootless fake capture -> three epochs -> size/age rotation -> incremental ETL -> final canonical session -> inspect/replay; prove one-shot equivalence, no duplicates, bounded resources, safe rollover-loss accounting, and payload-safe outputs. Depends on Gate D, 1-7. **Proof:** `cargo test --workspace --all-features --locked`.
- [ ] 8.2 `[fault/property tests]` Run durable crash matrix around frame/marker sync, manifest, epoch handoff, delta publication, checkpoint, final session, processed transition, cleanup, metadata, and signal drain; assert convergence/no silent loss/unsafe deletion/lease race. Depends on 8.1. **Proof:** checked-in deterministic fixtures/property seeds and exact recovered lineage assertions.
- [ ] 8.3 `[storage/ETL conformance]` Run filesystem RecordingStore conformance, checkpoint fork/corruption, synchronized and separate quota domains, disk-full at every persistence boundary, and P1 regression suites. Depends on 8.1-8.2. **Proof:** retained CI logs summarized by scenarios, not privileged claims.
- [ ] 8.4 `[scripts/acceptance/p2-privileged.sh, scripts/tests, evidence]` Create clean-exact-commit Ubuntu 24.04 privileged acceptance under systemd `Type=simple`: real cgroup workload/eBPF; readiness polling; at least three epochs and size+age rotations; live incremental worker; standalone ETL live rejection; SIGTERM; process kill/restart at WAL/publication/checkpoint/final-session/cleanup points; one full host/VM reboot with persistent state followed by systemd restart/recovery before readiness; quota/min-free pressure; corruption quarantine; inspect/replay; process/cgroup/eBPF/temp cleanup. Depends on 8.1-8.3. **Proof:** retained machine-readable pre-reboot/post-reboot report/artifacts/checksums under `evidence/privileged/<commit>/...` with kernel/arch/capabilities/unit/config digest and checked/not-checked claims.
- [ ] 8.5 `[scripts/acceptance/p2-multipass.sh]` Add reproducible clean-tree VM runner that clones exact SHA, executes 8.4 across required VM reboot/resume, hashes/transfers evidence, and refuses dirty or mismatched commit. Depends on 8.4. **Proof:** retained successful supported-environment run including host-reboot recovery.
- [ ] 8.6 `[CI/tooling]` Run formatting, locked workspace check, Clippy all targets/features with warnings denied, all-feature tests, isolated eBPF build, acceptance-runner tests, and targeted LSP/static diagnostics. Depends on 8.1-8.5. **Proof:** all commands pass; fixture-only lanes label privileged runtime not checked.
- [x] 8.7 `[OpenSpec]` Run `openspec validate add-continuous-recorder-foundation --strict`; confirm proposal/design/spec/task consistency, implementation-gate evidence references, and checkbox evidence. This is SDD validation only and does not satisfy 8.4-8.5. Depends on artifact/task updates. **Proof:** strict validator passes.
- [ ] 8.8 `[independent review]` Review durability authority, checkpoint/publication/final-session ordering, cleanup/quota safety, deterministic equivalence, storage ownership, privilege/sensitive-data posture, and acceptance evidence. Resolve every blocker before archive. Depends on 8.4-8.7. **Proof:** retained review findings/dispositions and no unresolved blocker.
