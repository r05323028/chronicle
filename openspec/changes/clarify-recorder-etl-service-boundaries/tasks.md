## 1. Architecture documentation

- [x] 1.1 Update `docs/architecture.md`: rewrite the recording-pipeline/ETL overview to describe the implementation accurately and concisely (incremental ETL during recording over the committed prefix, canonical delta batches, durable incremental checkpoints, epoch rollover, cross-epoch continuation, multi-epoch parent recordings, final authoritative one-shot ETL/publication), fix the Boundaries diagram so `chronicle-etl` lists `storage` among its dependencies, qualify `.chronicle-domain.lock` as a local filesystem/domain coordination mechanism, and add a short service-boundaries subsection naming Recorder, Local WAL, Durable Evidence Store, ETL, and Canonical Store with the co-location, remote-evidence-store handoff, and replay-independence statements.
- [x] 1.2 Update `docs/architecture/crate-boundaries.md`: replace the "Current graph (implementation start, 2026-08-11)" block (which still shows `chronicle-session -> chronicle-wal` and `chronicle-cli` to lower-layer crates as current problem edges) with the actual current Cargo graph, which equals the existing Target graph; historical dependency problems belong in Git history and archived OpenSpec changes, not in the current architecture contract. Keep crate ownership, semantic boundaries, and reliability sections intact.
- [x] 1.3 Add durable rules to `AGENTS.md` (concise, no duplication of the full specification): local WAL is the capture durability authority and remote evidence stores are handoff boundaries; Recorder and ETL are separate logical boundaries and co-location is an implementation detail whose correctness must not depend on sharing a process, memory, or capture ownership; ETL remains independently deployable in the architecture and owns canonical publication, publication verification, and checkpoint advancement ordering; architecture changes require architecture documentation and validation-policy review together.

## 2. Policy and validation

- [x] 2.1 Verify `validation/architecture.toml` is unchanged: no dependency graph changed and the existing allowlist already represents the desired crate direction; do not modify the policy file and do not add speculative checks for future S3, Kubernetes, or service-deployment concepts.
- [x] 2.2 Validate: `openspec validate --all --strict --no-interactive` passes; `./scripts/validate.sh fast` passes; documented crate graph compared against actual `cargo metadata --format-version 1 --no-deps` output; canonical documentation searched for stale statements (one final ETL only, one recording always producing one session, Recorder and ETL necessarily sharing a process, `.chronicle-domain.lock` as a universal distributed lock); `AGENTS.md`, `docs/architecture.md`, and `docs/architecture/crate-boundaries.md` agree on the durable architecture rules.
