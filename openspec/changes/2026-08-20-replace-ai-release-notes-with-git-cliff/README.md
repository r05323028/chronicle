# Deterministic release notes with git-cliff

This change replaces model-generated release notes with a reproducible Git-history renderer. Implementation artifacts and validation are tracked in this directory.
