## 1. Release-note prompt

- [x] 1.1 Add `.github/prompts/release-notes.md`: Markdown-only prompt that instructs the agent to inspect the checked-out repository and git history itself — determine the current release from the checked-out Git tag, determine the previous semantic-version release tag (or treat absence as the initial public release), inspect repository documentation and implementation for what actually shipped, focus on user-facing functionality (installation; `record` → `list`/`inspect` → `replay`; `doctor`; Linux eBPF capture; durable WAL behavior; ETL/canonical session behavior; deterministic replay; replay safety; supported protocol scope; meaningful known limitations), distinguish shipped from planned functionality, avoid claiming unsupported protocols/storage backends/deployment modes, ignore non-user-visible internal refactors, avoid dumping raw commit history, and produce concise developer-oriented notes using exactly `## Highlights`, `## Installation`, `## What's included`, `## Known limitations` — with no surrounding code fence, no introductory commentary, and no trailing commentary. The file SHALL NOT contain GitHub Actions expressions or a precomputed commit list. Acceptance: prompt file exists, is non-empty, and the workflow does not inline it.

## 2. Release workflow

- [x] 2.1 Define workflow-level env in `.github/workflows/release.yml`: `OPENCODE_GO_BASE_URL=https://opencode.ai/zen/go/v1`, `RELEASE_NOTES_MODEL=deepseek-v4-flash`, `RELEASE_NOTES_THINKING_LEVEL=off`; reference these values throughout instead of duplicating literals.
- [x] 2.2 Add `validate-tag` job: checkout, run `scripts/release/validate-tag.sh "$GITHUB_REF_NAME" Cargo.toml` (script derives the workspace version from the manifest, normalizes the tag's leading `v`, exits nonzero on mismatch). `release` and `release-notes` depend on it.
- [x] 2.3 Add `scripts/release/validate-tag.sh` (bash, stdlib tooling) implementing D5.
- [x] 2.4 Add `scripts/release/write-opencode-go-models-json.py` (python3 stdlib) implementing D2: writes `~/.pi/agent/models.json` with provider `opencode-go` (`baseUrl` from `OPENCODE_GO_BASE_URL`, `api` = `openai-completions`, `apiKey` = literal `$OPENCODE_GO_API_KEY` env-var name reference, `authHeader` = true, model id from `RELEASE_NOTES_MODEL`); fails on missing env vars.
- [x] 2.5 Add `release-notes` job (D3): checkout `fetch-depth: 0`; configure the provider via 2.4; load `.github/prompts/release-notes.md` into a step output; run `shaftoe/pi-coding-agent-action@v2.27.0` with `github_token`, `provider: opencode-go`, `model`/`thinking_level` from workflow env, `load_builtin_extensions: false`, `prompt` from the loaded file, and `OPENCODE_GO_API_KEY` injected only into that step's env; save `steps.generate.outputs.response` to `release-notes.md`; non-empty check; upload `release-notes.md` artifact. Fail closed — no fallback.
- [x] 2.6 Update `publish`: `needs: [release, release-notes]`; add `GH_TOKEN`/`GH_REPO` env; download `release-notes` artifact; replace `--generate-notes` with `--notes-file release-notes.md --verify-tag`.
- [x] 2.7 Update `checksums`: add `GH_TOKEN`/`GH_REPO` env; keep compose + `sha256sum -c` verify + upload; add explicit assertion that the release assets include `SHA256SUMS` (release not complete without it).
- [x] 2.8 Permissions: keep `contents: write` only on `publish`/checksums; validate-tag/release/release-notes read-only. Acceptance: workflow parses, no `--generate-notes` anywhere, every gh job has GH context.

## 3. Validation wiring

- [x] 3.1 Add `scripts/tests/validation/test_release_workflow.py` (stdlib unittest, D9) covering: prompt file exists; prompt not inlined in release.yml; no `--generate-notes`; publication consumes `release-notes.md` (`--notes-file`, `--verify-tag`); every gh-invoking job declares `GH_TOKEN`/`GH_REPO`; read-only jobs hold no write permission; tag/workspace-version mismatch fails (functional, via the script); OpenCode Go provider config is env-driven (functional, via the script, temp HOME).
- [x] 3.2 Register the new test in `scripts/validate.sh` (fast `tooling-tests` + gate-tooling-tests lines) and in `validation/test-architecture/test-catalog.toml` (`tooling:validation-tests` command). Acceptance: fast and targeted tooling validation run the new suite.

## 4. OpenSpec and archive

- [x] 4.1 Archive `add-shell-installer-for-release-binaries`: sync its delta specs into the main specs (shell-installer, release-packaging checksum manifest, layered-validation, developer-onboarding-documentation), verify, then move the change into `openspec/changes/archive/`. Acceptance: strict OpenSpec validation passes; archive contains the change with its `.openspec.yaml`.
- [x] 4.2 Run strict OpenSpec validation and bounded fast validation. Acceptance: `openspec validate --all --strict --no-interactive` and `./scripts/validate.sh fast` pass with the new workflow tests; diff review shows only workflow/prompt/scripts/tests/OpenSpec changes and no product behavior change.

## 5. Release dry-run mode

- [x] 5.1 Add `workflow_dispatch` to the release workflow; a manual dispatch resolves release context in dry-run mode (version + intended tag derived from the root `Cargo.toml`, no Git tag required, malformed version fails) and runs the full preparation path: build matrix, Pi release notes, archive collection, `SHA256SUMS` generation and verification, release-notes collection, prepared-bundle upload, and a job summary stating mode/version/tag/archives/notes/checksums/publication status with the explicit "Dry run completed successfully. No GitHub Release was created." line. Acceptance: dry-run context script is unit-tested; workflow tests assert dispatch support and the summary step.
- [x] 5.2 Gate publication to the real-release boundary: the publish job (and all release-mutating `gh` commands) run only when `github.event_name == 'push'` AND `startsWith(github.ref, 'refs/tags/v')`; `workflow_dispatch` can never reach `gh release create`. Acceptance: workflow tests assert the gate expression and the absence of release-mutating `gh` outside publish.
- [x] 5.3 Consolidate version/tag parsing into `scripts/release/resolve-release-context.sh` (supersedes `validate-tag.sh`): release mode requires `v<workspace-version> == tag` (mismatch fails), dry-run mode emits `mode/version/tag/is-release` from the manifest. Wire the workflow's resolve-context job to it and emit job outputs. Acceptance: functional tests cover matching tag, mismatched tag, missing tag, dry-run resolution, malformed/missing version, missing manifest.
- [x] 5.4 Add `prepare-release` job: collect archives + `release-notes.md`, assert the expected archive set (`chronicle-<version>-<target>.tar.gz` for both targets), generate and verify `SHA256SUMS` before publication, upload the `prepared-release` bundle, and write the run summary. Acceptance: publish depends on `prepare-release` and attaches `SHA256SUMS` at release creation; no checksum composition happens after publication.
- [x] 5.5 Update the release-note prompt to handle tag-less dry-run context (workspace version from `Cargo.toml` as the intended release) without forking the production path or inlining the prompt. Acceptance: prompt file remains external, contains no GitHub Actions expressions, and workflow tests pass.
- [x] 5.6 Extend `test_release_workflow.py`: workflow_dispatch supported; publication gated to real tag push; no release-mutating `gh` outside publish; checksums generated+verified before publication; prepared-release bundle uploaded; dry-run version resolution functional tests; only `publish` holds `contents: write`. Acceptance: suite passes under fast validation and catalog command.
