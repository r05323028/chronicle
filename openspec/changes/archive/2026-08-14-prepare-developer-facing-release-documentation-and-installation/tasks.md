## 1. Release packaging foundation

- [x] 1.1 Add clap `version` to the `chronicle` command derive so `chronicle --version` prints `chronicle <workspace-version>` (e.g. 0.1.0); no new public subcommand and no behavior change to existing commands. Acceptance: `chronicle --version` exits 0 and reports the workspace version; existing CLI contract tests pass unchanged; `--version` does not appear as a subcommand in help.
- [x] 1.2 Add `.github/workflows/release.yml` triggered on `v*` tags: matrix of native Linux runners (x86_64 on ubuntu-latest, aarch64 on ubuntu-24.04-arm); build the eBPF object from `ebpf/` source (nightly + rust-src + bpf-linker, `-Z build-std=core`, `--target bpfel-unknown-none --release`, `--locked`, mirroring the existing unprivileged compile job); then `cargo build --release --features linux-ebpf --locked` so the built object is embedded through the existing `build.rs` path; package `chronicle-<version>-<target>.tar.gz` containing the `chronicle` binary, `LICENSE`, and a short README pointer; generate `SHA256SUMS`; create a GitHub Release with the archives and checksums attached. Acceptance: dry-run of the workflow steps on a Linux runner produces both archives plus checksums, and the release binary reports version and passes `doctor` capture.object probe.
- [x] 1.3 Extend the existing unprivileged eBPF-compile CI job to assert the tracked fallback object `crates/chronicle-capture-ebpf/objects/chronicle-ebpf-capture-bpfel.o` is current with `ebpf/` source (ancestry check: the object's last commit must be at least as new as the last commit touching `ebpf/` source; byte comparison is not used because the embedded object carries absolute build paths and nightly codegen varies by toolchain). Regenerate and commit the tracked object from the current source so the no-nightly contributor build embeds the same programs as release builds. Acceptance: CI fails when the tracked object drifts from `ebpf/` source; documentation states the regeneration command. Evidence: drift was detected on main (`a3d9d9e` changed `ebpf/src/main.rs` after the object's last regeneration in `5d2d60d`); the object was regenerated from source in this change.

## 2. README restructure: onboarding document

- [x] 2.1 Rewrite `README.md` to the target hierarchy: banner, positioning statement, What is Chronicle?, Current capabilities / status, Installation, Requirements, Quick Start, How It Works, Architecture, Supported Protocols, Safety, Current Limitations, CLI, Development, Documentation, License. Ordering principle What -> Install -> Quick Start -> Architecture -> Details; no internal implementation detail appears before installation and the first working example. Acceptance: structure check (headings present and ordered) passes; review confirms no P1-report content in the primary path.
- [x] 2.2 Write the positioning statement ("Chronicle records real application traffic with eBPF and turns it into deterministic, replayable regression-test evidence" refined to the implementation) plus supporting principles: minimal application-side integration, durable WAL persistence before interpretation, ETL to protocol/storage-independent canonical representation, replay decoupled from capture, safe replay that never automatically targets recorded production destinations. No unproven claims (zero overhead, zero code changes, arbitrary production safety). Acceptance: wording review; README claims are traceable to implementation (doctor probes, CLI contract, replay-safety docs).
- [x] 2.3 Write `## Current capabilities / status`: five public commands (record, replay, list, inspect, doctor), plaintext HTTP/1.1 capture via eBPF on Linux, bounded WAL + canonical session + local storage, safe loopback replay; explicit statement that PostgreSQL/S3 persistence, TLS, and non-HTTP/1.1 protocols are not implemented. Acceptance: capability list matches `chronicle --help` and docs/operations.md; no planned feature is presented as supported.
- [x] 2.4 Write `## Installation` with the binary install path (GitHub Release artifacts, archive contents, checksum verification) and `## Build from source` section: `cargo build --release --features linux-ebpf` with prerequisites (toolchain from rust-toolchain.toml, Linux 6.1+, cgroup v2, BTF, x86_64/aarch64, CAP_BPF/CAP_NET_ADMIN); note that the tracked eBPF object makes the feature build work without nightly; document the nightly rebuild path (rust-src, bpf-linker, `-Z build-std=core`) for changing `ebpf/` source. Remove the bare `cargo run -p chronicle-cli -- record ...` examples from the primary path; contributor demos use `cargo run --features linux-ebpf -p chronicle-cli -- ...` or the installed binary. Acceptance: every documented live-recording command enables the feature path; source-build steps execute successfully on Linux in CI validation.
- [x] 2.5 Write `## Requirements`: Linux 6.1+ (live capture), cgroup v2, BTF, x86_64/aarch64, eBPF privileges; `chronicle doctor` as the readiness check; non-Linux builds support the portable surface only. Do not duplicate low-level preflight detail. Acceptance: requirement list matches doctor probe codes and docs/operations.md bounds.
- [x] 2.6 Write `## Quick Start` on the installed binary: `chronicle doctor`; `chronicle record --name checkout -- ./my-app`; representative traffic note; `chronicle list`; `chronicle inspect checkout`; `chronicle replay checkout -- ./my-app`. Advanced forms (PID/cgroup selectors, explicit target, allow-host, effect gates, retry, recovery, WAL sizing, recorder internals, hidden compatibility commands, deprecated syntax) are excluded from Quick Start and link to docs/operations.md. Acceptance: each command form matches the validated CLI contract (see section 5); advanced forms do not appear in the Quick Start block.
- [x] 2.7 Write `## How It Works` + `## Architecture` presenting the flow (Application -> eBPF Capture -> WAL -> ETL -> Canonical Session; Canonical Session feeds Inspect and Storage; Replay consumes Storage) and responsibilities of capture, WAL, ETL/session reconstruction, canonical model, storage, protocol adapters, replay. Preserve explicitly that replay consumes canonical sessions and protocol interfaces and does not depend on capture, eBPF, WAL, or ETL. Link docs/architecture.md and docs/architecture/crate-boundaries.md; do not duplicate the crate dependency graph. Acceptance: replay boundary statement present and not oversimplified; links resolve.
- [x] 2.8 Write `## Supported Protocols`: compact matrix with supported (HTTP/1.1), planned (PostgreSQL, MySQL, MariaDB, MongoDB, Kafka, NATS), research (Oracle Net/TNS); statement that a registered protocol is scaffolding, not support; internal test scaffolds such as Fake excluded from the public table; S3-compatible artifact storage out of scope. Acceptance: statuses match current implementation (only Fake and bounded HTTP/1.1 functional end to end; Fake not in the public table).
- [x] 2.9 Write `## Safety` and `## Current Limitations`: sensitive production data in artifacts; replay side effects, dry-run default, deny-by-default effects; replay never automatically targets recorded production destinations; visible authorization/policy gates; no encryption at rest / redaction / tenant isolation; Linux-only live capture; plaintext HTTP/1.1 only; bounded recording (duration + WAL ceiling); local-filesystem storage; preserve-timing modeled not executed. Link docs/replay-safety.md and docs/operations.md for detail. Acceptance: all safety essentials present and concise; detailed semantics not duplicated.
- [x] 2.10 Write `## CLI` (intent of record, list, inspect, replay, doctor, plus global options; advanced forms link to docs/operations.md), `## Development` (toolchain, workspace build/test/gates, `./scripts/validate.sh fast` as canonical local validation; privileged acceptance opt-in only), `## Documentation` links (architecture, operations, replay-safety, contributing), `## License` (Apache-2.0). Acceptance: command overview does not replicate `--help`; referenced docs exist and resolve.
- [x] 2.11 Reclassify and remove from README: hidden 0.1.x compatibility entrypoints, deprecated syntax as primary presentation, WAL-format/schema-version policy, sidecar/catalog mechanics, recording bounds detail, P1-specific machinery, and the fixture-based macOS demo via hidden forms; move to docs/operations.md or docs/architecture.md where not already present. Hidden compatibility commands are not presented as a product interface. Acceptance: README reads as product onboarding; grep-audit shows no primary-path reference to hidden compatibility forms in the README.

## 3. Documentation separation and links

- [x] 3.1 Ensure README links target existing documents (docs/architecture.md, docs/architecture/crate-boundaries.md, docs/operations.md, docs/replay-safety.md, docs/release-notes.md, CONTRIBUTING.md); move any README-only operational content into the appropriate doc without rewriting its semantics. Acceptance: link-existence assertion (part of section 5 contract test) passes for every README relative link.
- [x] 3.2 Update CONTRIBUTING.md only if the README restructure changes contributor entry points (e.g., add `./scripts/validate.sh fast` reference if absent); no scope creep. Acceptance: contributing guidance remains accurate and references the canonical validation entry point.

## 4. Validation wiring

- [x] 4.1 Add rootless documentation-command contract test `tests/smoke/test_documented_commands.py` (black-box, built binary via existing support helpers): assert `--version` output and exit 0; `--help` lists the five public commands and hides internal mechanics; `doctor` runs non-destructively with actionable output (capture probes unsupported in a non-feature build); `list` empty contract; public record-syntax (`record --name NAME -- CMD...`) on a non-feature build fails preflight with typed error and no mutation; fixture-recorded public inspect/replay forms where rootless-compatible; README relative links resolve. No prose matching of README text. Acceptance: test passes rootless on macOS/Linux CI; failure on any documented command contract break.
- [x] 4.2 Wire the documentation-command contract into validation: extend the `cli_docs` case in `scripts/validate.sh` to run the new test, and add it to the CI checks job smoke invocation list (tests/** already selects the portable group for fast/targeted). Acceptance: `./scripts/validate.sh targeted --changed-since origin/main` with README/docs changes runs the contract test and selects no privileged gate; CI runs it on every push/PR. Note: fixed a pre-existing `validation.py` path-matching bug (`path.lstrip("./")` stripped the leading dot of `.github/**` paths, misclassifying workflow changes as unknown and forcing conservative full validation); `removeprefix` restores correct group ownership (`build_tooling`).
- [x] 4.3 Add privileged quick-start scenario (P1, shared owner) executing the exact documented forms against a release-built linux-ebpf binary: `chronicle doctor` (capture probes supported), `chronicle record --name checkout -- ./my-app` with representative traffic from the existing HTTP driver, `chronicle list`, `chronicle inspect checkout`, `chronicle replay checkout -- ./my-app` PASS; assert published recording, catalog entry, inspectable session, replay verification, and no recorded-destination contact. Acceptance: scenario passes in P1 privileged profile and produces retained machine-readable evidence. Note: privileged validation surfaced two product-behavior facts the scenario and README must reflect — (1) live captures are at least partially replayable, never guaranteed fully (missing_timestamp warnings are normal; replay executes complete operations and skips degraded ones, outcome completed_with_skips); (2) command-mode replay refuses the exact recorded destination (denied(recordeddestination)), so a quick start must record a non-loopback destination and replay into a loopback copy on the same port. Scenario verified green against a release-built linux-ebpf binary in the chronicle-ubuntu VM (doctor supported; record -> list -> inspect fully_replayable -> replay completed).
- [x] 4.4 Add release-artifact validation steps to the release workflow: run `chronicle --version`, `--help`, and `doctor` (assert capture.object/embedded object probe) on the release binary; verify `SHA256SUMS` matches archive contents. Acceptance: release artifacts fail closed if version, help, object embedding, or checksums are broken.

## 5. Final validation

- [x] 5.1 Run strict OpenSpec validation and bounded fast/targeted validation. Acceptance: `openspec validate --all --strict --no-interactive` passes; `./scripts/validate.sh fast` and targeted pass; documentation-command contract test passes.
- [x] 5.2 Run required privileged P1/P2 scenarios on supported Linux for the quick-start and release-binary paths. Acceptance: retained machine-readable evidence for the privileged quick-start scenario; release binary passes doctor and the scenario.
- [x] 5.3 Confirm no production behavior changed beyond `chronicle --version`; existing CLI contract tests, smoke, acceptance, and e2e suites pass unchanged. Acceptance: test suites green; diff review shows no WAL/ETL/canonical/storage/replay/capture/recorder behavior changes.
