## 1. Capture current-state evidence

- [x] 1.1 Record the semantic-leak inventory with file:line references: application re-export lines (`crates/chronicle-application/src/lib.rs` 72 common, 73 protocol, 74-77 replay block), CLI production usage (`crates/chronicle-cli/src/main.rs` 22, 376, 418-424, 906, 1498, 1541, 1845-1847), CLI test usage (1856, ~2191-2274), and view-model fields (`replay_inspect.rs` ReplaySessionResult/ReplayOperationSummary/InspectSessionResult at lines 78, 428-436, 484-494). Append to design.md "Leak" section; the inventory must match main at implementation time (re-verify, do not assume).
- [x] 1.2 Verify the Cargo boundary is unchanged and strong: `chronicle-cli` depends only on `chronicle-application`; `scripts/validation.py architecture` passes on current HEAD. Record output.
- [x] 1.3 Measure and record AGENTS.md size (~17 KB) and enumerate duplicated prose vs `validation/test-architecture/README.md` and `docs/architecture/crate-boundaries.md`. Record the intended navigation-map structure.
- [x] 1.4 Record the sleep-policy enforcement reality: `scripts/tests/validation/test_sleep_policy.py` enforces the no-magic-sleep/unbounded-polling rule and is wired into `validate.sh fast` via the tooling-tests step, but the rule is documented nowhere outside the test file.
- [x] 1.5 Record source-size distribution and logging reality as taste evidence (largest files; no `tracing` in production crates; sparse `eprintln!`). No hard thresholds introduced.
- [x] 1.6 Record current exit-code/JSON contract for replay output (REPLAY_REPORT_VERSION, outcome strings, dry_run/preflight_denied/transport_failed, counts; exit codes 0 completed/dry-run, 3 cleanup timed out, 4 policy/invalid, 5 transport, 6 verification, plus the error family) as the compatibility baseline for contract corrections.

## 2. Classify the semantic leaks

- [x] 2.1 Classify each leak from 1.1 as production leak, test-only leak, view-model leak, or intentional adapter contract. Outcome must match design.md tables (production: LoopbackReplayOptions construction, ReplayOutcome matching, TimingMode conversion, replay_outcome_exit_code via outcome; test-only: ProtocolError/TransportErrorCategory/OperationExecutionState/Replayability construction; view-model: ReplaySessionResult.outcome/replayability, ReplayOperationSummary.state; intentional: protocol_registry() pass-through, chronicle-common primitives).
- [x] 2.2 Decide which view-model fields must change type/accessor vs stay opaque, preserving serialized JSON. Record the decision that `InspectSessionResult.replayability` remains a documented intentional serialized-only contract (CLI never names it; application render functions own presentation) unless review decides otherwise. Record rationale.
- [x] 2.3 Confirm no other outer adapter (none exists today) and no other lower-layer vocabulary crosses the CLI boundary beyond the inventory (grep the full CLI crate, not only main.rs, including tests/cli_contract.rs).

## 3. Specify the policy and canonical docs

- [x] 3.1 Extend `validation/architecture.toml` with the `[semantic]` table: `forbidden_outer_vocabulary` (the eight symbols), `forbidden_re_export_sources` (all lower-layer crates except chronicle-common), empty `allowed_re_exports`. Document the add-entry convention (rationale + doc update in same change).
- [x] 3.2 Add the semantic-boundary model section to `docs/architecture/crate-boundaries.md`: dependency vs semantic boundary, the five invariants, the intentional-contract list, and the policy pointer. Update `AGENTS.md` crate-architecture rules only if wording changes are required to stay consistent with the spec requirement.
- [x] 3.3 Create `docs/engineering-taste.md`: the 7 taste principles with enforcement classification, the promotion ladder (recurring finding -> documented principle -> observed repetition -> promoted invariant -> actionable remediation), the four promotion criteria, the validation-message format (invariant/location/rationale/remediation), and the current mechanical-invariant inventory (architecture.toml, semantic check, sleep policy, catalog, preflight, timeout wrapper). Document the sleep policy here so it is no longer code-only.
- [x] 3.4 Add the bounded-command-execution parameter table to `CONTRIBUTING.md` under a new "Validation timeouts" section (modes + env-var knobs + defaults), preserving every knob currently in AGENTS.md.

## 4. Correct application-facing contracts (compatibility-preserving)

- [x] 4.1 Remove the verbatim `pub use chronicle_replay::{...}` and `pub use chronicle_protocol::{...}` lines from `crates/chronicle-application/src/lib.rs`; keep `pub use chronicle_common::{...}`. Confirm nothing else in the workspace (tests, scripts) depended on the re-export path.
- [x] 4.2 Re-type the application-owned view model: `ReplaySessionResult` outcome/replayability and `ReplayOperationSummary` state become an application-owned classification type with serde representation identical to today (required, not accessor-only: the name scan cannot catch opaque field access such as `result.outcome`). Existing JSON/human output and exit codes must remain byte-identical; assert with before/after fixture comparisons.
- [x] 4.3 Introduce the application-owned replay request API: CLI passes plain request data (target, allow_hosts, execute, allow_read/write, timing choice) and application constructs `LoopbackReplayOptions` internally. CLI's `impl From<Timing> for TimingMode` conversion and both `LoopbackReplayOptions { ... }` constructions (main.rs 1498, 1541) move behind it.
- [x] 4.4 Replace CLI success determination and exit mapping: `render_public_replay_human` no longer matches `ReplayOutcome` variants; `replay_exit_code` uses an application-owned `replay_result_exit_code(&ReplaySessionResult)`-style function instead of extracting `result.outcome`.
- [x] 4.5 Move exit-code mapping tests (`replay_outcomes_map_to_stable_exit_codes`, `application_errors_map_to_stable_exit_family`) from CLI tests into chronicle-application's test modules (taste principle 4: shared invariants have one owner). CLI tests keep rendering/wiring checks and use application-owned fixture constructors (create application-owned fixture helpers if none exist); CLI must no longer construct `ReplaySessionResult` with replay-owned enum fields. Land 4.1/4.4/4.5 in one commit sequence (removing re-exports breaks CLI test imports until these land).
- [x] 4.6 Optional, only if trivial: application-owned wrapper for `process_and_publish_recording_wal` so `protocol_registry()` stays application-internal; otherwise document `protocol_registry()` as the reviewed access seam in the crate-boundaries semantic section.
- [x] 4.7 Run the full workspace test suite plus CLI integration/smoke/acceptance/E2E suites; all existing tests must pass unchanged (except the relocated mapping tests). Confirm CLI JSON output and exit codes unchanged via the cli_contract and root black-box suites.

## 5. Introduce the mechanical validator and tests

- [x] 5.1 Extend `scripts/validation.py` `architecture` subcommand to run semantic checks when `[semantic]` is present: re-export scan over `crates/chronicle-application/src/**` covering `pub use <crate>::{...}` blocks (continuation-join multiline), bare `pub use <crate>;`, and glob `pub use <crate>::*;` forms (prefix-match `pub use <forbidden_crate>`), flagging symbols not allowlisted; vocabulary scan over `crates/chronicle-cli/src/**` and `crates/chronicle-cli/tests/**` (word-boundary exact matches of forbidden symbols after stripping `//`/`///` comment lines, file:line); and policy self-consistency (each forbidden symbol resolves to a forbidden re-export source; allowlist entries well-formed).
- [x] 5.2 Violation messages follow the format: invariant, location, rationale, remediation (with doc pointer). Add a tooling test asserting all four elements are present in violation output. The format requirement applies to checks added under this change; the legacy sleep checker names the violation and location and may be upgraded opportunistically.
- [x] 5.3 Extend `scripts/tests/validation/test_architecture_boundaries.py` (or add `test_semantic_boundaries.py`) with temp-workspace fixtures proving: acceptance (application-owned result type consumed by CLI passes; chronicle-common primitives re-export passes; internal application modules using lower-layer types pass); rejection (`pub use chronicle_replay::ReplayOutcome;` in application fails; `use chronicle_application::{ReplayOutcome,...}` in CLI fails; `LoopbackReplayOptions` construction in CLI fails); false-positive control (identifier containing a forbidden name passes, comment-line mention passes, bare `pub use <crate>;` and glob forms are caught).
- [x] 5.4 Run the tooling self-tests; existing architecture tests continue passing (regression: valid Cargo edges unchanged).
- [x] 5.5 Confirm the extended `architecture` step runs under the existing bounded command timeout in `validate.sh fast` and the release path with no new step.

## 6. Simplify AGENTS.md after canonical docs exist

- [x] 6.1 Condense AGENTS.md to the ~5-6 KB navigation map per design.md table: compressed philosophy, CodeGraph/graphify pointers, condensed bounded-execution safety rule (pointer to CONTRIBUTING.md), condensed reliability-boundaries bullet list, entrypoint list (validate.sh modes, openspec, acceptance.sh), macOS/Multipass note, OpenSpec workflow pointer, condensed task-completion rules, mandatory crate-architecture summary (ownership one-liners + dependency direction + forbidden patterns + update-in-same-change rule), pointer to validation/test-architecture/README.md replacing duplicated Test Architecture prose, and a navigation map table linking canonical docs.
- [x] 6.2 Verify every removed detail still exists in a linked canonical location (CONTRIBUTING.md, test-architecture README, crate-boundaries.md, engineering-taste.md); no guidance is deleted silently.
- [x] 6.3 Verify the existing spec requirement (workspace-dependency-boundaries "Architecture guidance is durable for future agents") still holds: AGENTS.md retains a mandatory crate architecture section (summary + pointers).
- [x] 6.4 Grep AGENTS.md and the linked docs for contradictory statements (spec: guidance and policy must agree); update wording where the condensation changed meaning.

## 7. Prove the final state via validate.sh fast

- [x] 7.1 Run `./scripts/validate.sh fast` end to end; the architecture step (now including semantic checks) passes, workspace tests pass, tooling self-tests pass, OpenSpec strict validation passes.
- [x] 7.2 Run a targeted rejection probe locally: temporarily add a forbidden CLI usage in a scratch file under crates/chronicle-cli/src/ or crates/chronicle-cli/tests/ (must be inside the scanned scope; not committed), confirm the architecture step fails with a message containing invariant/location/rationale/remediation, then remove it and confirm green. Record the probe output as evidence.
- [x] 7.3 Confirm `./scripts/validation.py architecture --root .` output is deterministic and the `[semantic]` policy is reported in the check summary.

## 8. Sync specs and finish

- [x] 8.1 Sync the change's spec deltas to canonical specs (`openspec/specs/workspace-dependency-boundaries`, new `openspec/specs/engineering-taste`, `openspec/specs/developer-onboarding-documentation`) and validate with `openspec validate --all --strict --no-interactive`.
- [x] 8.2 Update `docs/architecture/crate-boundaries.md` "Current graph" note and AGENTS.md only where the semantic model changes their wording; do not alter dependency allowlists.
- [x] 8.3 During the implementation phase, after code changes, run `graphify update .` (AST-only) and re-run the architecture/OpenSpec checks; record the updated graph commit.
- [x] 8.4 No task above is marked complete without its evidence: test output, probe output, or a recorded before/after artifact. Privileged acceptance is not required for any task in this change; do not run privileged gates to prove portable source/API policy.
- [x] 8.5 Record that this change edits acceptance-fingerprint inputs (`crates/**`, `scripts/validation.py`, `scripts/validate.sh`, `validation/**`), which invalidates retained live-capture/recorder evidence under the content-addressed evidence policy; release planning must regenerate evidence before any release gate.
