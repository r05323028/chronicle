# installer fixtures

Static pieces used by `tests/smoke/test_installer.py` to build fake release
trees (archives + SHA256SUMS) served over a local HTTP server. No real
GitHub access is required.

- `chronicle-stub` — fake `chronicle` binary placed inside fixture archives.
